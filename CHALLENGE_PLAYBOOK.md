# 技能编程关卡功能与流程清单

## Purpose

- 本文档是同类算法关卡的功能点和玩家流程事实源，用于新题简报、实现对照、交付审查和回归验收。
- `AGENTS.md` 定义方法论与工程边界，`FRONTEND.md` 管理组件复用，本文档管理玩家应该获得的标准功能与完整流程。
- 功能的世界文案、场景动画和题目语义可以不同，但空状态、锁定、验证失效、批次校验、错误返回和阶段解锁等通用契约不得由每道题临时发挥。

## Maintenance Rules

- 新关卡立项时，在题目简报中引用本文档的功能 ID，并标明必选功能的实现位置和验收用例。
- 一个功能只有在产品中可操作、在题目契约中可声明、在浏览器中可验收时，才能在覆盖矩阵中标为“已完成”。
- 新增、删除或改变必选功能时，同一任务必须更新本文档、受影响题目的契约/实现、浏览器验收和 `会话协作记录.md`。
- 题目不适用某个必选功能时，必须在题目简报中写明算法或学习原因，并在开发前由用户确认；不允许以“这次没做”作为豁免。
- 本文档登记的是产品功能，不等于已经存在正式共享组件；组件状态以 `FRONTEND.md` 为准。

## Product Entry Flow

1. 软件根路径直接显示挑战目录，不增加营销落地页；目录条目由统一清单声明标题、算法、学习专题、数据结构、解题技巧、难度、开放状态、语言、阶段数、预计时长、前置关卡，以及显式的预览与路径状态。已开放关卡必须提供真实游戏预览和可达路径。
2. 玩家选择挑战后直接进入该题的“动手理解”阶段；开发验收可以保留代码阶段直达参数，但不得成为正常玩家默认入口。
3. 关卡普通阶段与代码实战阶段都必须提供“挑战选择”返回路径；该入口不能替代代码实战返回技能调试等题内返回关系。
4. 目录可以记录“上次进入”的挑战以降低返回成本，但未接入真实完成状态前不得把访问记录展示为关卡进度或通关状态。

## Catalog V2 State And Scale

- `available`：关卡已通过当前 Definition of Done，可选择并进入。
- `coming-soon`：可以展示真实、已确认的信息，但不可伪装为可玩；若只有概念而无可验收页面，不使用虚构游戏截图。
- `locked`：存在明确且可验证的前置关卡；界面说明解锁条件，不使用目录顺序假定玩家已完成前置内容。
- `intro`：围绕一个主要概念完成直接组合，前置知识少；`intermediate`：需要协调多个状态、分支或作用域；`challenge`：需要组合多个已学概念或处理更复杂的不变量。难度描述本产品中的学习复杂度，不照搬外部题库评级。
- 1-4 道题默认使用真实预览主导的精选卡片；不因预留未来能力展示空筛选栏。
- 5-12 道题默认按学习专题组织，并提供数据结构、解题技巧、难度和开放状态的基础筛选。
- 超过 12 道题时默认增加标题/算法搜索、排序和真实“继续学习”入口；是否分页或虚拟化由实际性能和浏览成本决定，不提前固定。
- 桌面与移动端可以使用不同密度和控件形态，但筛选结果、空结果、状态含义和可进入性必须一致。
- 以上题量是默认设计触发线，可在真实内容和浏览器验收支持下调整；任何调整都要写回本文件、`FRONTEND.md` 和协作记录。

## Canonical Stage Flow

| Stage | Entry gate | Required player work | Completion gate | Required return path |
| --- | --- | --- | --- | --- |
| 01 动手理解 | 首屏直接进入 | 在小数据上操作或标记，看到目标状态与约束 | 真实场景结果正确 | 可继续修改当前场景 |
| 02 技能认识 | 动手理解通过 | 浏览少量核心技能，理解其逻辑作用；细节按需预演 | 预演不改变正式程序和批次结果 | 可返回动手理解 |
| 03 规则编排 | 技能阶段可用 | 从空程序开始，通过点选/拖放构建技能树 | 程序结构、参数和作用域契约可执行 | 可清空并重新编排 |
| 04 调试执行 | 当前程序可执行 | 播放、暂停、单步、重置，根据具体错误修改 | 当前批次的真实输出正确 | 重置后可回到编辑状态 |
| 05 多批验证 | 当前程序非空且可校验 | 可逐批播放，也可使用“校验全部批次” | 同一套当前程序通过所有正式批次 | 失败可查看首个失败批次，成功可重新验证 |
| 06 代码实战 | 所有正式批次通过 | 把已验证的技能树翻译为目标语言 | 公开与隐藏用例的真实结果全部正确 | 可返回技能调试 |

## Feature Registry

`Required` 表示默认题型必须提供；`Conditional` 表示只在题目含有对应概念时必须提供。

| ID | Level | Feature | Canonical contract | Browser acceptance |
| --- | --- | --- | --- | --- |
| `catalog.challenge-selection` | Product | 挑战目录 | 根路径只展示已登记且状态真实的挑战；已开放条目使用真实游戏预览，非开放条目不得伪装成可玩链接 | 桌面与手机可进入每个已开放挑战，并正确识别开发中和锁定状态 |
| `catalog.challenge-return` | Product | 返回挑战选择 | 普通阶段与代码阶段均可回到根目录，不破坏题内返回路径 | 两题普通阶段和代码直达入口均可返回目录 |
| `catalog.metadata-v2` | Product | 可扩展目录元数据 | 每题声明专题、数据结构、技巧、难度、状态、语言、阶段、预计时长、前置关卡，以及显式预览/路径状态；目录不读取题内算法状态 | 长标题、多标签、缺失字段、条件必填和无效枚举有自动校验，新增题只通过登记数据进入目录 |
| `catalog.truthful-state` | Product | 真实开放与进度状态 | 开放、开发中和锁定状态来自真实产品事实；最近访问不等于学习进度 | 三种状态的文案、可点击性和前置条件正确，不展示虚构完成度 |
| `catalog.scalable-browse` | Product | 题量自适应浏览 | 1-4 题精选展示，5-12 题专题分组与基础筛选，13 题及以上增加搜索、排序和继续学习 | 按当前题量层级验证适用控件、结果、空结果、长列表及桌面/手机无溢出 |
| `catalog.route-integrity` | Product | 目录与路由完整性 | 已开放目录项必须同时存在页面入口、生产构建入口、真实预览和题内返回路径 | 自动检查登记唯一性与路由可达性，浏览器可进入并返回每道已开放关卡 |
| `manual.direct-entry` | Required | 关卡首屏动手理解 | 进入某个挑战后不增加营销页，直接操作小数据 | 关卡首屏可操作，完成条件来自真实结果 |
| `skill.core-library` | Required | 核心技能架 | 只保留玩家需要理解和排序的算法骨架，默认 4-7 个；代码级微操作不单独占卡 | 不打开全部预演也能从短名称和一句逻辑作用看懂编排顺序 |
| `skill.detail-expansion` | Required | 技能内部展开 | 核心技能可在执行时展开读取、判断、状态更新和边界移动等真实步骤 | 单步执行仍能看到完整因果链，但规则树节点数保持精炼 |
| `skill.preview` | Required | 技能预演 | 预演不写入程序、正式数据或验证进度 | 逐项打开/重播/关闭，程序和批次状态不变 |
| `mapping.concept-inventory` | Required | 算法概念清单 | 输入输出、所有有含义变量、运算、控制流、边界和 API 均有稳定角色 ID | 参考代码中任一结果相关概念都可在映射清单中定位 |
| `mapping.state-lifecycle` | Required | 关键状态生命周期 | 关键变量明确初始值、读取点、更新时机、更新规则和可见特征 | 单步执行能看到旧值、触发原因、新值及其场景来源 |
| `mapping.causal-chain` | Required | 完整因果链 | 状态更新、公式计算和结果写回在内部执行中保持可观察，不强迫玩家逐个编排 | 运行帧按真实顺序展示操作数、运算、新值和写回目标 |
| `mapping.stage-parity` | Required | 跨阶段同源映射 | 场景、核心技能或其内部帧、参考步骤、词典、槽位和语义检查显式关联同一概念 ID | 从任一参考步骤可追溯到前置核心技能/执行帧和右侧输入，反向也成立 |
| `program.empty-default` | Required | 程序初始为空 | 进入编排阶段时不预填技能、参数或参考答案 | 程序节点数为 0，空状态中性，运行和全批校验禁用 |
| `program.place-click` | Required | 点选放置 | 技能可先选中，再选明确插入位置 | 根节点和嵌套作用域均可放置 |
| `program.place-drag` | Required | 拖放与重排 | 支持新技能放置、同层重排、跨层移动和移回技能架 | 桌面鼠标与移动端触控均通过 |
| `program.scope-contract` | Conditional | 显式作用域 | 循环/判断提供可见子区域，动作只能进入满足能力契约的位置 | 错误关联具体技能或参数 |
| `program.clear` | Required | 清空程序 | 清空后回到空程序与中性状态 | 无残留帧、批次通过或错误高亮 |
| `program.edit-invalidates-verification` | Required | 编辑作废旧验证 | 新增、删除、移动或修改任何技能后，清除旧帧和全部批次通过记录 | 修改已通过程序后不能直接进入代码阶段 |
| `runtime.playback` | Required | 播放/暂停/单步/重置 | 操作同一份真实执行帧，重置回到待命 | 暂停帧不自动前进，单步只前进一帧 |
| `runtime.edit-lock` | Required | 运行锁定 | 播放或未重置的中间帧不允许改变程序 | 预演、新增、删除和移动均禁用，重置后恢复 |
| `batch.sequential` | Required | 逐批验证 | 玩家可观看每批的完整因果过程 | 当前批次通过后可进入下一批 |
| `batch.verify-all` | Required | 一键校准（界面名“校验全部批次”） | 对玩家当前程序直接运行所有正式批次；不自动填入、修复或替换程序 | 空程序时禁用；成功一次标记全部批次；失败不解锁代码阶段 |
| `batch.inspect-failure` | Required | 查看失败批次 | 全批校验失败时显示失败数量与首个可操作原因，可进入首个失败批次逐帧调试 | 不只显示“校验失败”的全局结论 |
| `batch.reverify` | Required | 重新验证 | 全部通过后可清除验证进度，保留当前程序重跑 | 回到第一批待命，代码阶段重新锁定 |
| `code.reference` | Required | 完整标准参考 | 零输入即按技能树顺序显示可组合方法体 | 每段可复制，不自动写入编辑器 |
| `code.dictionary` | Required | 原子代码词典 | 场景、技能、语法、数据结构/API 独立映射 | API 能力不只藏在长片段中 |
| `code.dual-mode` | Required | 结构/自由双模式 | 默认结构填写，自由模式只编辑方法体，草稿互不覆盖 | 切换、刷新恢复和当前模式重置通过 |
| `code.run-submit` | Required | 当前用例运行/全部用例提交 | 语法、语义和真实执行分层检查；隐藏用例不提前展示，失败后只揭示首个失败反例 | 错误聚焦槽位或代码行；首个失败反例展示输入、期望、实际和原因；正确结果通过全部用例 |
| `feedback.completion-celebration` | Required | 通用通关反馈 | 仅在代码实战从未通关进入公开与隐藏用例全部通过时自动展示；用六阶段算法回路闭合表达完整学习旅程，不导入题目 ID、场景实体或算法专用动画 | 桌面与手机显示正确通过数量；可重播、关闭和按 `Esc` 跳过；关闭后保留通关结果；减少动态效果时直接呈现稳定终态 |
| `responsive.primary-task` | Required | 响应式主任务 | 桌面使用双工作区，窄屏为主观察目标提供切换或纵向流程 | 无横向溢出、遮挡和不可达主操作 |

## Default State Contract

### Entering Program Stage

- `program = []`：技能树必须为空，界面只显示中性空状态和放置入口。
- `frames = []`、`passedBatches = []`、`suiteCheck = null`：不显示旧执行、旧错误或旧成功。
- 播放、单步和“校验全部批次”禁用；清空程序禁用。
- 核心技能架与技能预演保持可用，玩家从第一个逻辑块开始构建；不得要求先阅读所有技能长说明才理解主流程。
- 不提供“恢复初始挑战”、“装入标准答案”或任何会预填技能树的玩家功能。标准程序只能存在题目 QA fixture/测试中。

### Editing Program

- 任何程序改动都立即停止播放，清除当前帧、直接校验结果和所有批次通过记录，并重新锁定代码实战。
- 编辑后保留玩家的当前技能树，不自动修复、补全、重排或替换为标准程序。
- 程序为空时保持中性；玩家明确运行或校验后，才显示具体可修正错误。

### Direct Batch Verification

- “一键校准”的标准界面文案为“校验全部批次”，避免让玩家误以为系统会改动程序。
- 它只是当前程序的快速验证入口，不是答案注入、自动修复或跳过编排。
- 空程序时按钮禁用；运行中或处于未重置中间帧时禁用。
- 成功时一次性写入全部批次通过状态并解锁代码实战；失败时清晰标出失败批次，不写入全部通过。
- 失败结果必须提供“查看第 N 批/段执行”，返回真实执行帧继续调试。

### Final Challenge Completion

- 通关事实只来自 `code.run-submit` 的真实结果：公开与隐藏用例全部通过后，先写入通关结果，再展示通关动画；动画播放、结束或关闭都不能反向决定通关。
- 自动展示只发生在当前关卡由“未通关”进入“已通关”的状态转换时；重复提交、刷新或返回结果页不得自行循环播放。动画内部可以重播，开发验收可以使用显式预览入口。
- 通用视觉固定表达六阶段“动手理解 -> 技能认识 -> 规则编排 -> 调试执行 -> 多批验证 -> 代码实战”，将程序结构和验证信号汇入算法核心并形成“关卡通关”结论。
- 动画只消费通过数量、总用例数量和关闭命令，不读取题目 ID、题目场景、技能名称、算法变量或专用文案；世界叙事和题目成功结果继续留在原页面。
- 玩家可以重播、点击关闭命令或按 `Esc` 跳过；关闭后回到仍然可见的通关结果，不自动跳转目录、不清空代码、不改变草稿和测试结果。
- 使用语义化模态对话框并把初始焦点放在可关闭控件上；系统启用“减少动态效果”时跳过长序列，直接呈现完整可读的稳定终态。

## Challenge Coverage Matrix

| Feature group | 移动零 V2 | 接雨水 | 和为 K 的子数组 | 最小覆盖子串 | 无重复字符的最长子串 | 找到字符串中所有字母异位词 | Required evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 产品目录选择与任意阶段返回 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 根目录选择、普通阶段/代码阶段返回及桌面/手机验收 |
| 动手理解与阶段门槛 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 主流程浏览器验收 |
| 精炼技能架、内部展开与无污染预演 | 已完成 | 已完成 | 已完成（5 项） | 已完成（6 项） | 已完成（5 项） | 已完成（5 项） | 玩家技能默认 4-7 个；不读全部预演可理解顺序；内部帧保持完整；预演状态隔离 |
| 概念清单、状态生命周期与跨阶段映射 | 待按新规范复核 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 概念覆盖自动测试、关键状态变化帧和前后阶段追溯验收 |
| 空程序默认状态 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 首次进入节点数为 0，运行/校验禁用 |
| 点选、拖放、嵌套、重排与清空 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 桌面与移动端编排验收 |
| 播放、暂停、单步、重置 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 帧进度与运行锁定验收 |
| 逐批验证 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 三批真实输出回归 |
| 校验全部批次、失败查看与重新验证 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 成功/失败分支、解锁与失效验收 |
| 代码标准参考与词典 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 零输入、独立复制与 API 词典验收 |
| 结构/自由双模式与双草稿 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 刷新、重置隔离和自定义变量验收 |
| 公开/隐藏用例真实提交 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 全通过与隐藏失败保密验收 |
| 通用通关反馈 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 真实全部提交成功触发、通过数量、重播、关闭、`Esc`、结果保留、减少动态效果及桌面/手机验收 |
| 桌面/窄屏主任务 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 已完成 | 截图、溢出和遮挡检查 |

## Product Catalog Coverage

| Feature | Current status | Required next evidence |
| --- | --- | --- |
| 挑战选择、真实预览和任意阶段返回 | 已完成 | 新题接入时继续执行桌面与手机回归 |
| Catalog V2 分类、难度、状态、时长和前置元数据 | 已完成 | 六题字段、枚举、开放状态、路径和预览校验通过 |
| 1-4 题精选卡片 | 已完成历史层级验收 | 当前已进入 5-12 题资料库层 |
| 5-12 题专题分组与基础筛选 | 已完成六题验收 | 三个专题、五类筛选、组合空态及桌面/手机无溢出通过；新增题继续回归 |
| 13 题及以上搜索、排序和继续学习 | 未触发 | 第 13 道可展示题进入目录前完成实现；继续学习必须依赖真实进度 |
| 开放、开发中和锁定状态 | 契约已确认，待实现 | 首次出现非开放题或下一道新题接入时完成状态交互验收，以先发生者为准 |

## New Challenge Delivery Checklist

### Before Implementation

- [ ] 新挑战已登记到统一目录清单，包含稳定 ID、顺序、专题、数据结构、技巧、难度、真实状态、语言、阶段数、预计时长、前置关卡，以及显式预览/路径状态；`available` 必须有真实预览和独立可达路径。
- [ ] 当前题量若达到 5 题或 13 题设计触发线，已先完成对应专题分组/筛选或搜索/排序方案；不得把新题塞入不再适用的旧目录布局。
- [ ] 完成题目简报，标明本文档中所有 Required/Conditional 功能的适用性。
- [ ] 从标准解法反向列出概念清单：输入/输出、每个有含义变量、运算、控制流、不变量、边界和 API。
- [ ] 为每个关键状态写明“场景特征 -> 标准变量 -> 初始化 -> 读取 -> 更新触发 -> 更新规则 -> 结果用途”。
- [ ] 建立概念覆盖表，明确每个概念在场景、核心技能或内部运行帧、参考、词典、槽位和语义检查中的对应项；不得为了覆盖表完整而把每个概念都变成技能卡。
- [ ] 核心技能默认控制在 4-7 个，短名称顺序本身能概括算法；超出时已先完成认知压缩审查并记录不可合并的学习理由。
- [ ] 定义空的技能程序初始状态，正确程序只进入 QA fixture。
- [ ] 在题目契约中声明批次、程序结构约束、直接校验能力和编辑后验证失效规则。
- [ ] 对照 `FRONTEND.md` 确认可直接复用、待评估和题目专用的前端边界。

### During Implementation

- [ ] 六阶段入口、完成门槛和返回路径已连通。
- [ ] 规则编排首次进入为空，空状态中性，操作禁用条件正确。
- [ ] 编辑程序会清除旧帧、旧批次结果和代码阶段解锁。
- [ ] “校验全部批次”只验证当前程序，失败可回到首个失败批次调试。
- [ ] 标准参考、结构 scaffold、语义验证和真实用例消费题目事实源。
- [ ] 每个关键状态在前置阶段持续可见，每次更新有明确执行帧；同一核心技能内部可以展开多个状态变化，但不得暗中跳过。
- [ ] 公式帧显示真实操作数及其来源，并与左侧参考步骤和右侧槽位一一对应。
- [ ] 全部公开与隐藏用例首次通过后触发 `feedback.completion-celebration`；动画关闭不会清除通关结果、代码草稿或测试状态。

### Before Delivery

- [ ] 从根目录进入新挑战、从普通阶段和代码阶段返回目录均完成桌面与手机浏览器验收。
- [ ] 目录登记与状态一致；已开放关卡的生产构建入口、真实预览和页面路由齐全且可进入，开发中/锁定关卡的预览、路径与可点击性符合条件契约。
- [ ] 当前题量对应的目录精选、分组、筛选、搜索、排序、空结果和长标题状态已按适用范围验收。
- [ ] 空状态、正确编排、典型错误、逐批验证和全批校验均完成真实浏览器操作。
- [ ] 全批校验成功、失败、失败批次查看、重新验证和编辑失效均有断言。
- [ ] 代码实战的初始/重置中性、空格输入、错误聚焦、双草稿、刷新、公开/隐藏提交均通过；多用例失败时只展示首个失败反例的输入、期望、实际和原因，且移动端无横向溢出。
- [ ] 通关反馈完成真实成功触发、六阶段内容、通过数量、重播、关闭、`Esc`、关闭后结果保留和减少动态效果验收；预览参数不能替代真实通关流程。
- [ ] 逐项对照标准参考：所有结果相关变量和运算都能追溯到前置场景、核心技能或内部执行帧；核心技能都能追溯到一段完整参考、槽位和语义检查。
- [ ] 桌面和窄屏无重叠、遮挡和横向溢出。
- [ ] 更新本文档覆盖矩阵、`FRONTEND.md` 正式登记表/复用队列和 `会话协作记录.md`。
- [ ] 旧题、V1/V2、共享组件使用方和生产构建按影响范围回归通过。
