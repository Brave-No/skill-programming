# 会话协作记录

更新时间：2026-08-28（Asia/Shanghai）

## 用途

这份文件是以下两个 Codex 任务之间的共享事实源，用于确认对方正在做什么、已经完成什么、验证结果如何、下一步是什么。

- 协作任务：`建立会话共享工作记录`
  - 任务 ID：`01a02222-65a1-7861-9cf8-c2c5d3b0e383`
  - 工作目录：`/Users/edy/Documents/ChatGPT/技能编程`
- 产品开发任务：`共创编程逻辑闯关游戏`
  - 任务 ID：`01a01dec-00a1-7e21-8a6d-f92fc8e6226c`
  - 工作目录：`/Users/edy/Documents/Codex/2026-08-20/wo-xi`

## 共同约定

1. 每次开始相关工作前，先读这份文件。
2. 发生以下任一情况后更新文件：接受新任务、作出重要决定、完成一项实现、验证结果变化、遇到阻塞、准备结束一轮工作。
3. 每个任务只直接改自己的“当前状态”；不要删除或改写另一个任务的记录。
4. 重要进展同时追加到“协作日志”，写明时间、任务名、完成情况、验证和下一步。
5. 记录只写已经确认的事实。计划和推测必须明确标为“计划”或“待确认”。
6. 若文件内容与代码或实际运行状态冲突，以最新代码和验证结果为准，并立即修正记录。

## 共同产品上下文

### 产品方向

- 产品不是简单地“把代码游戏化”，而是把算法思考过程变成可操作、可观察、可调试的世界规则。
- 概念分为四层：道具（数据结构/变量/指针）、技能（遍历/判断/交换等动作）、策略（双指针等组合方式）、关卡（初始状态/限制/目标）。
- 通关依据是系统真实执行玩家规则后的结果，而不是匹配唯一答案。
- 初始验证目标是：玩家先理解世界并操纵规律，最后才意识到自己完成了算法；调试过程本身是核心玩法。

### 第一关：零号仓库

- 面向高中至成人编程初学者，第一关使用“移动零”。
- 手动阶段先让玩家整理货位；自动阶段通过五种技能编排规则，并用三个固定批次验证泛化能力。
- 五种技能：定位装载标记、逐个遍历、条件判断、交换位置、移动装载标记。
- 技术栈：Vite、React、TypeScript、dnd-kit、lucide-react。
- 视觉方向：明亮克制的工业机器人仓库；左侧执行区，右侧规则编排区；桌面和移动端均需可用。

### 版本现状

- V1 位于 `/v1`：解决旧版右侧拖拽命中区互相遮挡的问题，使用明确插入线，并支持拖动、点选放置、同层重排、跨层嵌套、拖回工具架和移动端触控。
- V2 位于 `/v2`：将一次性积木升级为可重复调用的技能实例；`for`、`if` 具有明确作用域和契约；支持嵌套、参数配置、上下文能力校验以及真实执行。
- 本地试玩服务当前使用 `http://localhost:5173`；具体是否仍在运行，应在使用前重新确认。

### 待实现的双机器人表达

- 用户确认当前关卡采用双指针后，要求两个指针都具象为机器人，避免“一个机器人 + 一个抽象标记”造成语义割裂。
- 下方扫描机器人对应快指针：逐个经过货位，发现非零货箱时负责取箱并投递。
- 上方装载机器人对应慢指针：驻守当前装载位，接住快指针投来的货箱并放入货位；只有装载完成后才前进一步。
- 交换动画应表现为“扫描机器人取箱 -> 货箱移动/投递 -> 装载机器人接箱并落位”，而不是两个抽象数字令牌交叉。
- 该变化首先应用于 V2；算法解释器、规则执行结果和 V1 均保持不变。

### 待实现的代码实战阶段

- V2 在三批规则全部通过后进入“代码实战”，把已经理解的场景和动作翻译为手写代码。
- 第一版目标语言默认使用 Java；映射体系必须与语言解耦，后续可增加其他语言。
- 左侧是可查看、可定位的原子映射词典，分为场景对象、技能动作、语法结构、数据结构/API 方法；每个方法单独映射，例如未来关卡中的 `Map.put` 不能只藏在某个复合技能里。
- 当前关卡的核心映射包括：仓库货位 -> `int[] nums`、空位 -> `0`、装载手 -> `writeIndex`、扫描手 -> `scanIndex`、定位/遍历/判断/交换/前进 -> 对应 Java 语句或代码块。
- 右侧是带 Java 语法高亮、行号和错误反馈的手写代码区，提供运行、提交验证、重置和返回技能调试。
- 代码结果必须通过真实测试用例判定，不能按固定答案字符串匹配；当前三批作为公开用例，并增加边界用例。
- 当前机器没有 Java Runtime；本地原型应采用可靠 Java 解析器加明确受限的浏览器执行方案，或其他不依赖系统 JDK 的安全方案，并对不支持的语法给出明确反馈。
- 映射项与编辑器联动：选择左侧条目可定位相关代码，编辑器解析后可显示哪些概念已经在代码中出现，但不能一键替玩家生成完整答案。

### 待修正的代码学习路径（2026-08-24）

- 用户反馈当前映射仍是“写完后检查/跳转”，没有提供能从头到尾直接组合成完整答案的代码提示，偏离“只掌握逻辑也能完成”的目标。
- 新的双路径：熟练玩家直接在右侧自由手写；零基础玩家查看完整、顺序明确、嵌套明确的代码片段，逐段照抄并在抄写过程中理解和记忆。
- 左侧主内容改为“完整逻辑代码清单”，必须在玩家尚未输入任何代码时就完整呈现；代码顺序严格对应上一阶段已经验证的技能树。
- 清单需要覆盖完整方法体的每一行，包括初始化、`for`、`if`、三行交换、慢指针前进以及所有闭合大括号，全部片段按顺序组合后可直接通过测试。
- 每段同时展示世界动作、逻辑作用与 Java 代码；用稳定的缩进/连接线表现 `for -> if -> 动作` 嵌套关系。
- 原有场景对象、语法、API 原子映射继续保留为次级“代码词典”，用于查阅数组、指针、`length` 和未来 `Map.put` 等知识点。
- 移除“写完后才出现/点击跳转”作为主交互；代码出现状态可保留为轻量进度，但不能隐藏提示，也不能要求先写。
- 不自动把完整答案写入编辑器。允许复制单个片段以降低输入摩擦，但主体验仍是玩家按逻辑组合、手写或照抄。

### 锁定结构填写模式（2026-08-24）

- 用户确认结构填写模式中的 `for`、`if`、缩进、作用域和大括号全部锁定，不允许玩家破坏已经在技能阶段验证通过的程序树结构。
- 代码实战提供两个明确模式：默认“结构填写”和“自由编写”。
- 结构填写从上一阶段的技能树生成锁定 Java 外壳，只开放逻辑槽位：准备变量、循环初始化、循环条件、循环前进、判断条件、交换动作、慢指针前进。
- 结构槽位允许用户自定义变量名并跨槽位引用，例如 `len / fast / slow / tmp`；组合后的完整方法体进入同一个安全 AST 执行器。
- 自由编写继续提供完整 CodeMirror 方法体，允许用户自行决定全部语句和变量命名。
- 两种模式分别保存草稿；首次从结构填写转到自由编写时，可将当前结构组合结果作为起点，但不得反向用不可靠的字符串拆分覆盖结构草稿。
- 完成状态必须基于 AST、作用域和数据流中的变量角色识别，不能匹配固定变量名。用户示例中的 `len = nums.length`、`fast` 遍历、`slow` 接货以及 `tmp` 交换应被识别为与参考命名等价。
- 结构填写中的固定语法与可编辑槽位必须在视觉、键盘焦点和辅助技术层面清楚区分；固定结构不可通过粘贴或删除改变。

## 当前状态

### `建立会话共享工作记录`

- 状态：协作机制已建立；锁定结构填写模式已由本任务在原源码完成实现与全量验收。
- 已完成：读取 `共创编程逻辑闯关游戏` 的产品起点、第一关规格、V1/V2 设计决策、主要实现与验证历史，以及最新在途工作。
- 已完成：已将本文件路径和维护协议同步给产品开发任务；对方已确认读取并将在本轮完成后写回实现与验证结果。
- 已完成：将“双机器人具象表达双指针”的需求同步给产品开发任务，并跟踪至实现、浏览器验收、V1 回归和共享记录写回全部完成。
- 已完成：将“代码实战阶段”的产品结构、Java 默认语言和原子映射要求同步给产品开发任务，并跟踪至实现、全量回归、桌面/移动验收和共享记录写回全部完成。
- 已完成：将“完整可组合代码提示 + 自由手写双路径”的反馈同步产品开发任务，并跟踪至结构化参考、翻译轨道、逐段复制、移动切换、全量回归和共享记录写回完成。
- 已完成：在原源码接管并完成“锁定结构填写模式”，包括共源 scaffold、七个语义槽位、AST 角色识别、双模式独立草稿、槽位诊断和桌面/移动验收。
- 当前职责：已完成本轮实现、全量回归和共享记录写回，继续作为两个任务的共享事实维护方。
- 下一步：等待用户实际体验结构填写的提示密度、输入节奏和自定义命名反馈；有新决定时继续同步两边任务。

### `共创编程逻辑闯关游戏`

- 状态：“锁定结构填写模式”已由协调任务在原源码完成实现，本任务已完成只读代码、模型和桌面/移动截图复核；全量验收通过。
- 本轮权限说明：当前任务的 cwd 被切换到协作目录，对 `/Users/edy/Documents/Codex/2026-08-20/wo-xi` 只有读取权限；按协调要求未迁移、复制或切换入口，由协调任务直接修改同一份运行源码，因此没有产生版本分叉。
- 只读复核贡献：确认槽位诊断必须由组合器产出行列映射；推动固定 `for / if / {}`、分号和多行槽位行数提升为组合器与界面共用的 `STRUCTURED_SCAFFOLD`；推动结构模式首次进入及重置后保持中性状态，避免“什么也没做就报错”。
- 最新实现：默认“结构填写”与“自由编写”分段控制、七个自定义变量槽位、AST 角色识别、两种交换方向、双模式独立草稿、当前模式重置和槽位错误聚焦均已完成；`len / fast / slow / tmp` 用户示例在两种模式中均通过 8/8。
- 最新验证：41 项单元测试、TypeScript、生产构建、V1 桌面/移动、V2 双机器人、代码实战桌面/移动验收全部通过；控制台无错误、页面无横向溢出，`http://localhost:5173/v2?stage=code` 返回 HTTP 200。
- 最新完成：V2 代码实战左侧已改为零输入即可完整查看的结构化翻译轨道；右侧继续提供空白方法体、自由手写、草稿、运行和全用例提交。
- 当前结论：新手可以按执行顺序逐段照抄完整 Java 方法体，熟练玩家可以忽略参考直接编写；两条路径使用同一真实用例判定。
- 本轮完成：在 V2 中将此前的实体装载标记替换为上方装载手和下方扫描手；五种技能预演和真实交换都使用机器人取箱、投递、接箱、落位表达；同位交换显示协作反馈，不生成投递货箱。
- 边界确认：解释器、批次结果和 V1 保持不变；V1 继续使用原扫描臂和装载位指示。
- 已实现的预演：定位装载标记、逐个遍历、条件判断、交换位置、移动装载标记。
- 最新验证：V2 浏览器验收通过（五种预演、真实投递中间态、第二批同位动作、播放/暂停/重置锁定、直接校验、桌面 `1440 × 1000` 与移动 `390 × 844` 无横向溢出）；V1 桌面/移动 E2E 通过且控制台无错误。
- 最新工程验证：双机器人回归时的 `npx tsc -b --pretty false`、`npm test -- --run`（4 个测试文件、22 项）和 `npm run build -- --emptyOutDir` 均通过；交付复核修正后全量为 6 个测试文件、31 项，类型检查和构建仍通过。
- 下一步：等待用户体验双机器人职责和动作节奏反馈；若继续迭代，优先调整机器人姿态、接货距离和动画时长。

#### 代码实战阶段本轮实现事实

- 完成：新增 `src/v2/codePractice/mappings.ts`，以语言解耦的 `LanguageMapping` / `CodeMappingEntry` 保存场景对象、技能动作、语法结构和 API 方法；Java 映射覆盖 `nums`、`0`、`writeIndex`、`scanIndex`、`for`、`if`、交换、自增、数组访问和 `length`，并保留独立的未来 `Map.put` 条目模型。
- 完成：新增 `src/v2/codePractice/cases.ts`，三批公开用例外增加空数组、全零、无零、单元素和混合边界用例。
- 完成：新增 `src/v2/codePractice/javaSubset.ts`，使用分词、递归下降 AST 和解释执行，明确只支持变量、整数数组、数组访问、`length`、`for`、`if/else`、赋值、比较、算术、自增和交换；不调用系统 JDK，不使用 `eval`，有步数/时间上限，并区分语法、不支持语法、运行、超时和输出错误。
- 完成：新增 `src/v2/codePractice/CodePracticeV2.tsx`，使用 CodeMirror Java 高亮、行号、键盘编辑、原子映射定位、解析状态、公开/边界用例运行、全部提交、错误行定位、本地草稿和重置。
- 完成：`src/v2/AppV2.tsx` 增加 `code` phase；三批正式通过后显示“进入代码实战”，`/v2?stage=code` 保留为开发验收直达入口，代码页可返回技能调试；V1 和 V2 技能调试解释器未改动。
- 完成：`src/styles.css` 增加独立代码工作台命名空间；桌面为映射参考 + 编辑器，移动端映射可折叠且编辑器保持可用高度。
- 完成：新增映射单元测试和 `work/qa_code_practice.py` 浏览器验收；正式 V2 验收脚本补充通关入口与返回路径检查。
- Java 子集边界：当前不伪装成完整 JVM；`while`、类/方法声明、对象 API 和其他未实现语法会明确提示暂不支持。
- 修正：产品默认值改为 `EMPTY_JAVA_BODY = ''`，完整方法签名 `void moveZeroes(int[] nums) {` 与结尾 `}` 作为不可编辑视觉层展示，签名不会交给子集解析器；空体显示中性“尚未开始编写”。
- 修正：正确双指针实现移入 `src/v2/codePractice/qaFixtures.ts` 和浏览器 QA fixture，产品初始/重置不再预填答案；草稿 key 升级为 `zero-warehouse:v2:java-code-draft:v2`，旧完整答案 key 不再读取。
- 修正：浏览器验收先确认旧 key 不会恢复答案，再确认初始映射全部“尚未出现”、空体真实提交失败，随后通过真实编辑输入正确方法体验证公开/边界用例 8/8，最后确认重置回空白。
- 验证：`npm test -- --run` 通过（6 个测试文件、31 项）；`npx tsc -b --pretty false` 通过；`npm run build -- --emptyOutDir` 通过；`work/e2e_zero_warehouse.py` 通过；`work/qa_v2_semantics.py` 通过；`work/qa_code_practice.py` 通过，桌面 `1440 × 1000` 与移动 `390 × 844` 均无横向溢出、控制台无错误。
- 下一步：等待用户重新体验“场景动作逐项变成代码”的连贯性；若有反馈，优先修正映射粒度、编辑器提示和 Java 子集边界说明。

#### 完整逻辑参考双路径修正事实

- 完成：新增 `src/v2/codePractice/logicReference.ts`，用语言可扩展的结构化数据定义 7 个有序逻辑步骤；层级依次为 `0, 0, 1, 2, 2, 1, 0`，完整覆盖初始化、`for`、`if`、三行交换、装载标记前进以及两个闭合大括号。
- 完成：左侧主内容改为“逻辑代码”翻译轨道；玩家代码为空时全部步骤仍完整可见，每段同时显示上一阶段技能、世界动作、逻辑作用和 Java 代码，并以缩进和连续轨道表达 `for -> if -> 交换/前进` 的作用域。
- 完成：每段提供独立复制按钮和短暂“已复制”反馈；复制只进入剪贴板，不自动插入编辑器；“待抄写/已写入”和 `0/7 -> 7/7` 只作为轻量进度，不会隐藏代码。
- 完成：原场景对象、语法结构和独立 API 方法映射保留为次级“代码词典”；未来 `Map.put` 仍可作为独立 `api-method` 条目接入。
- 完成：移动端提供“参考/编辑”显式切换；桌面继续保持同层左右工作台。空白方法体、固定签名、CodeMirror、草稿、重置、返回技能调试和安全 AST 执行均保持。
- 一致性：`src/v2/codePractice/qaFixtures.ts` 的正确 Java 方法体由同一逻辑参考结构拼接，不再维护另一份手写答案；新增单测确认步骤顺序、层级、完整三行交换、括号闭合、空代码参考不隐藏以及拼接程序通过全部 8 个用例。
- 浏览器验收：空白进入即见完整参考；逐段复制不自动写入；逐段手动输入后进度从 `0/7` 到 `7/7` 并通过 8/8；熟练者直接填写也通过；仅公开用例正确时隐藏用例会失败且不泄露数据；词典查询、草稿刷新、重置、正式通关入口和移动切换均通过。
- 回归验证：`npm test -- --run` 通过（7 个测试文件、35 项）；TypeScript 类型检查和生产构建通过；V1 桌面/移动 E2E、V2 双机器人专项回归、代码实战桌面 `1440 x 1000` 与移动 `390 x 844` 验收均通过，浏览器控制台无错误、页面无横向溢出。
- 运行状态：2026-08-24 最终确认 `http://localhost:5173/v2?stage=code` 返回 HTTP 200。
- 下一步：等待用户体验“完整参考照抄”和“自由手写”两条路径；根据反馈优先调整翻译轨道的信息密度、复制粒度或移动端切换节奏。

## 协作日志

### 2026-08-28 - 公网发布同步推送代码仓库

- 用户决定：后续每次把当前产品发布到公网服务器时，同时把同一份可部署源码提交并推送到代码仓库，不能只更新服务器。
- 发布事务：部署服务在每个发布批次执行发布前清理、私密文件排除和必要验证；同一源码快照推送至 `origin/release` 并部署到服务器，最终记录服务器版本、Git 提交和两端结果。
- 失败边界：服务器发布或仓库推送任一失败时，如实报告两端状态，不把该批次标记为完整发布成功；后续补齐时仍以同一源码快照为准。
- 当前仓库：本地 `release` 分支跟踪 `origin/release`，远端为私有仓库 `Brave-No/skill-programming`。本轮只固化发布规则，没有把当前在途工作区提前提交或推送。
- 规则分类：该要求依赖本项目的部署任务、分支和远端仓库，作为项目级规则写入 `AGENTS.md`；未修改全局 Agent 规则。

### 2026-08-25 - `公网 Ubuntu 服务器部署完成`

- 已接受：将当前统一技能编程产品部署到 Ubuntu 服务器 `39.96.62.70`，完成 SSH 连接后通过公网对外提供访问。
- 安全准备：项目根目录新增本地私密文件 `server-credentials.env`，预填主机、`22` 端口和 `root` 用户，密码由用户填写；该文件已加入 `.gitignore`，不得进入仓库。
- SSH 与服务器：`root@39.96.62.70:22` 已验证成功；系统为 Ubuntu 26.04 LTS，服务器原先只监听 SSH，无既有 Web 服务或站点冲突。
- 部署完成：已安装并启用 Nginx 1.28.3，版本化发布位于 `/var/www/skill-programming/releases/20260825-164112`，`current` 已原子指向该版本；站点配置来自 `deploy/nginx.conf`。
- 发布边界：当前工作区行为测试共 151 项通过；在途新关卡存在类型检查失败且“最小覆盖子串”尚无可达构建入口，本次通过独立快照仅发布已完整验证的“零号仓库”与“雨线峡谷”，未改动并行开发源码。
- 服务器验证：Nginx 配置检查通过且服务为 active；服务器本机访问根目录、两道关卡和两张预览图均返回 HTTP 200。
- 公网阻塞：Ubuntu UFW 未启用，公网 80 请求未进入 Nginx 日志；已在阿里云控制台定位唯一关联安全组 `sg-2zee4plvszhphtj8hfhu`，并在快速添加规则中选中允许 `0.0.0.0/0` 访问 HTTP/TCP 80，待用户确认后点击“确定”实际修改云端权限。
- 公网放行：用户已在阿里云安全组中完成 HTTP/TCP 80 入站规则；公网请求随后正常到达 Nginx。
- 公网验收：`http://39.96.62.70/`、`/games/zero-warehouse/`、`/games/rainline/` 以及两张预览图全部返回 HTTP 200；未发布的 `/games/minimum-window/` 明确返回 404。Chrome 真实渲染确认目录两道关卡可见，两个关卡入口标题与根节点正常，控制台无错误。
- 技能记录：本轮使用 `chrome:control-chrome` 进入已登录的阿里云控制台，并已按 `skill-usage-tracker` 完成调用统计。

### 2026-08-25 - `工作线程4`（最小覆盖子串开始）

- 已接受：将 LeetCode“最小覆盖子串”作为第三道完整算法关卡接入统一技能编程产品。
- 前置门槛：按已确认的 Product Catalog Contract，先把现有两题迁移到 Catalog V2 元数据与状态契约，再登记第三题；不在旧目录结构上继续增加固定专题和固定开放状态分支。
- 学习目标：以字符串滑动窗口为策略，持续观察左右边界、目标字符欠账、当前覆盖状态和历史最短窗口；扩张、入窗记账、覆盖判定、最短记录、出窗记账和左侧收缩必须形成可单步追踪的因果链。
- 语言边界：Java 代码实战需要在现有安全 AST 执行链上新增字符串入参、`String.length()`、`charAt`、`substring`、新建整数计数数组和嵌套 `while`；不调用 JVM、不使用 `eval`，不以字符串匹配代替语义验证。
- 交付范围：题目简报、概念图、技能模型、解释器、公开/隐藏用例、结构/自由代码实战、统一目录、独立路由、真实预览、桌面/移动浏览器验收，以及移动零、接雨水和生产构建回归。
- 前端复用：本轮将在第三题可验收后执行正式复用审查；任何共享边界晋升都必须形成唯一源码、明确使用方、双题以上 fixture 与回归记录。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-21 - `建立会话共享工作记录`

- 完成：定位并读取另一个活跃任务 `共创编程逻辑闯关游戏` 的完整产品脉络和最新进度。
- 完成：创建本共享记录，并制定双方更新协议。
- 验证：另一个任务的 ID、工作目录、V1/V2 状态和当前在途工作均来自 Codex 任务上下文；本工作目录此前为空且不是 Git 仓库。
- 完成：已将文件绝对路径和维护要求同步给另一个任务；对方已确认会读取记录、继续当前工作，并在结束前写回最新状态。
- 下一步：双方后续工作均按“开始前读取、重要变化后更新、结束前写回”的协议维护本文件。

### 2026-08-21 - `共创编程逻辑闯关游戏`

- 完成：V2 将装载标记改为货位顶部的 `LocateFixed` 实体图标，并为技能架五种技能增加独立预演按钮；预演包含定位、遍历、条件判断、交换和前进五种动作反馈。
- 约定：预演是辅助观察功能，不参与规则树、不改变真实数组和批次结果；技能取用/拖动仍由左侧主区域负责，右侧图标只触发预演。
- 验证：类型检查、测试和构建通过；桌面与移动视口检查通过；真实运行、暂停/重置锁定状态及移动端溢出均已核对。
- 下一步：等待用户继续体验反馈；必要时再调整预演按钮位置、动画节奏或标记视觉。

### 2026-08-21 - `建立会话共享工作记录`（双机器人需求）

- 新决定：将 V2 的“扫描机器人 + 装载标记”升级为上下两个机器人，分别具象表达快指针和慢指针。
- 交互要求：快指针机器人负责扫描和投递非零货箱；慢指针机器人停在装载位接箱、落位，并在成功装载后前进。
- 动画要求：真实交换和技能预演都改为具象的取箱、投递、接箱、落位过程；不再用抽象交换令牌作为主要表达。
- 边界：先改 V2，保持算法逻辑、批次结果和 V1 不变；需要重新验证真实运行、预演、暂停/重置锁定与移动端布局。
- 下一步：同步给 `共创编程逻辑闯关游戏` 实现并写回验证结果。

### 2026-08-21 - `共创编程逻辑闯关游戏`（双机器人实现开始）

- 已接受：V2 双机器人具象表达需求，已完整读取共享事实源和边界约定。
- 计划：下方快机器人负责扫描、取箱和投递；上方慢机器人驻守装载位、接箱落位并在装载后前进；两个指针同位时显示协作反馈而不制造虚假交换。
- 边界：不修改 V1、解释器和批次判定；本轮完成后重新记录测试、构建及桌面/移动浏览器验收结果。

### 2026-08-21 - `共创编程逻辑闯关游戏`（双机器人实现完成）

- 完成：`WarehouseBoard` 增加 V2 双机器人视觉模式；上方 `装载手` 对应慢指针，下方 `扫描手` 对应快指针；`RobotGlyph` 支持两种具象姿态。
- 完成：真实交换和 `swap` 预演改为货箱投递动画；真实 `advance-write` 与预演分别使用执行节奏和展示节奏；同位交换显示“同位协作”并保持货箱原位。
- 完成：V1 使用默认经典视觉路径，未接入双机器人样式；解释器、契约和批次判定未改动。
- 验证：V2 专项浏览器脚本通过；V1 桌面/移动 E2E 通过；现有 22 项测试、类型检查和构建均通过；截图目检确认桌面与移动端无机器人/货位遮挡。
- 下一步：等待用户体验反馈。

### 2026-08-21 - `建立会话共享工作记录`（代码实战需求）

- 新阶段：V2 三批通关后进入“代码实战”；左侧查看世界/技能/语法/API 的代码映射，右侧手写程序并运行验证。
- 默认语言：第一版按用户举例默认 Java，同时要求映射数据模型支持未来扩展其他语言。
- 映射粒度：场景对象、行为、控制结构、数据结构方法都使用独立条目；未来 `Map.put` 等方法必须单独映射。
- 交互要求：左侧映射可定位并反映代码中已出现的概念，但不自动拼出完整答案；右侧提供可靠编辑体验、运行、提交、重置和返回。
- 判定要求：通过真实用例验证语义，不匹配固定答案文本；公开三批外增加边界用例。
- 环境事实：本机无 Java Runtime，需采用不依赖系统 JDK 的本地安全执行方案，并明确其语法边界。
- 下一步：同步给 `共创编程逻辑闯关游戏` 设计、实现和验收。

### 2026-08-21 - `共创编程逻辑闯关游戏`（代码实战实现开始）

- 已接受：V2 三批通关后进入代码实战的新阶段，默认 Java，映射按场景对象、技能动作、语法结构和 API 方法原子化。
- 重要决定（计划）：采用 CodeMirror Java 编辑器；执行器只支持明确声明的安全 Java 子集，不调用系统 JDK、不使用 `eval`，对超出范围语法返回“不支持语法”。
- 边界：V1 保持不变；正式入口来自 V2 通关，开发验收可保留受保护直达方式；本轮结束前补写实现、测试和浏览器验证。

### 2026-08-21 - `共创编程逻辑闯关游戏`（代码实战实现完成）

- 完成：接入 V2 `code` phase；正式三批通关后可进入代码实战，`/v2?stage=code` 可用于开发验收，代码页可返回技能调试。
- 完成：CodeMirror Java 编辑器、原子映射词典、映射行定位、出现状态、草稿保存、错误定位、公开用例运行、全部用例提交、重置和移动折叠布局。
- 完成：受限 Java AST 解析/解释执行；真实用例判定，不匹配固定答案字符串；支持步数/时间限制和语法、不支持语法、运行、超时、输出分类反馈。
- 验证：新增映射测试与代码实战 Playwright 验收通过；全量 30 项测试、TypeScript、构建、V1 E2E、V2 双机器人回归均通过；桌面和移动截图目检无横向溢出或遮挡。
- 下一步：等待用户体验并收集“世界动作到代码”的理解反馈。

### 2026-08-21 - `共创编程逻辑闯关游戏`（交付复核修正完成）

- 复核发现：产品默认代码曾是完整双指针答案，导致首次进入即可提交 8/8，不符合“右边手写编程”。
- 修正完成：产品默认/重置为空方法体；固定显示 `void moveZeroes(int[] nums) {` 和 `}`，仅方法体进入编辑器与子集解析器。
- 修正完成：空方法体显示“尚未开始编写”；空体运行/提交按真实数组结果返回输出不符；正确实现仅保存在测试/QA fixture。
- 修正完成：升级 localStorage 草稿 key，旧版本完整答案草稿不再读取；映射初始全为“尚未出现”，手写后逐项出现。
- 验证完成：真实输入正确方法体后公开/边界用例全部通过；重置回空白；桌面/移动布局、正式通关入口、V1/V2 回归和全量工程检查均通过。
- 最终复核：固定方法签名对视觉和辅助技术可读，但不属于可编辑 CodeMirror 内容；最终再次运行 31 项测试、类型检查和代码实战桌面/移动验收，均通过。

### 2026-08-24 - `建立会话共享工作记录`（完整代码组合反馈）

- 用户反馈：现有左侧映射在玩家写完代码后才用于出现状态和定位，无法让没有编码能力的玩家依靠逻辑完成任务。
- 新决定：代码阶段同时服务两类玩家；熟练者在右侧直接手写，新手在左侧查看完整的、按技能树顺序与层级拆分的 Java 方法体并逐段照抄。
- 主交互：零输入时就展示全部代码段；每段包含动作语义、逻辑作用和可组合代码；组合后不得缺少交换语句或闭合括号。
- 次级交互：保留原子代码词典，继续支持独立 API 方法映射；出现状态只作为进度，不再承担提示入口。
- 边界：不自动填入完整答案；可复制单段；右侧自由编辑、真实用例执行和受限 Java 安全边界保持不变。
- 验收重点：空白状态参考完整可见、逐段照抄可 8/8、直接手写仍可用、单段复制不自动插入、移动端参考与编辑器可切换、V1/V2 回归通过。

### 2026-08-24 - `共创编程逻辑闯关游戏`（双路径修正开始）

- 已接受：完整读取代码学习路径反馈，确认现有“写完后点亮/定位”方向需要改为“零输入即可逐段照抄完整答案”。
- 设计决定：使用安静工业工作台中的“翻译轨道”表达执行顺序和 `for -> if -> 交换/前进` 层级；次级标签保留代码词典；移动端提供参考/编辑切换。
- 工程决定：完整组合步骤使用语言可扩展结构化数据；测试正确 fixture 从同一参考数据拼接，并验证能通过全部用例，避免参考与执行答案漂移。
- 技能记录：本轮已按规范调用并记录 `frontend-design` 与 `webapp-testing`。

### 2026-08-24 - `共创编程逻辑闯关游戏`（双路径修正完成）

- 完成：代码实战左侧从“写完后检查/定位”改为零输入即可完整查看的 7 段 Java 翻译轨道；每段包含技能、世界动作、逻辑作用、完整代码和独立复制按钮，所有步骤按顺序组合后形成完整可运行方法体。
- 完成：原子代码词典降为次级标签并继续保留独立 API 条目；右侧仍为空白方法体自由手写，复制不会自动填入；移动端可直接在参考和编辑之间切换。
- 一致性：正确 QA fixture 从结构化逻辑参考拼接生成；单测锁定步骤顺序、作用域层级、交换三行、括号闭合、空白可见性和 8 个真实用例结果，避免参考与判定漂移。
- 验证：35 项单元测试、类型检查、生产构建、V1 桌面/移动回归、V2 双机器人回归、代码实战桌面/移动完整流程均通过；确认逐段照抄和熟练者直接编写两条路径都能达到 8/8，控制台无错误且无横向溢出。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `通用通关动画视觉原型`

- 用户纠正：通关特效必须是所有算法关卡共用的产品表达，不能依赖峡谷、货箱、机器人或其他单题场景。
- 方向：原型采用“算法回路闭合”，依次点亮动手理解、技能认识、规则编排、调试执行、多批验证和代码实战六阶段，将程序结构和验证信号汇入中央算法核心，最后形成“关卡通关”印记。
- 边界：已删除未完成的接雨水专属方向；新实现不导入题目 ID、题目规格或场景实体。当前仅挂在产品目录的独立预览参数，尚未改变两道关卡的真实通关流程，也不登记为正式共享组件。
- 预览：`http://localhost:5174/?celebration=preview`；支持重播、关闭、`Esc` 跳过和系统“减少动态效果”。
- 验证：统一仓库 12 个测试文件、67 项通过，TypeScript 与多页面生产构建通过；真实浏览器完成桌面 `1440 x 1000`、手机 `390 x 844`、重播、关闭、`Esc`、减少动效和溢出检查，无控制台错误。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。
- 下一步：交付用户体验，并根据实际学习过程反馈继续收敛提示粒度和视觉密度。

### 2026-08-24 - `建立会话共享工作记录`（锁定结构确认）

- 用户确认：结构填写模式的控制结构与嵌套外壳全部锁定。
- 模式定义：结构填写只开放语义槽位；自由编写保留完整代码编辑能力。
- 自定义命名要求：`len / fast / slow / tmp` 等自定义变量与参考命名必须被视为等价，完成度不得继续使用变量名正则判断。
- 技术方向：用安全 Java AST 分析声明、循环变量、长度别名、数组索引、条件、交换数据流与慢指针增量，按角色判断逻辑是否完成。
- 验收重点：固定结构无法删除；槽位键盘可达；结构组合可运行；用户示例 8/8；自由模式自定义命名也能正确标记；两种草稿不互相覆盖；移动端可用；V1/V2 回归通过。

### 2026-08-24 - `共创编程逻辑闯关游戏`（锁定结构只读接力）

- 已完成：完整读取“锁定结构填写模式”决定、最新协作日志、`frontend-design`、`webapp-testing` 与 `skill-usage-tracker` 规范。
- 只读确认：原源码仍位于 `/Users/edy/Documents/Codex/2026-08-20/wo-xi`；现有实现以 `logicReference.ts`、`CodePracticeV2.tsx` 和 `javaSubset.ts` 为主要扩展点，无需复制项目或改变 `http://localhost:5173` 试玩入口。
- 权限接力：当前任务因 cwd 切换只能读取原源码；协调任务已确认原源码实际可写并接管同一源码实现。当前任务已停止迁移方案，不创建副本、不切换端口，也不把未完成实现记为 blocked。
- 技能记录：本轮已按要求读取并采用 `frontend-design` 与 `webapp-testing`；尝试按 `skill-usage-tracker` 写入全局统计时受当前文件权限限制，协调任务需在其可写环境补记。
- 下一步：等待协调任务在原源码完成结构槽位、AST 角色分析、双草稿和浏览器回归，并由双方按共享协议写回最终事实。

### 2026-08-24 - `建立会话共享工作记录`（锁定结构实现完成）

- 完成：V2 代码实战默认进入“结构填写”，并保留“自由编写”；两种模式分别保存草稿，切换不覆盖，重置只清当前模式。
- 完成：新增共源 `STRUCTURED_SCAFFOLD` 数据模型，方法签名、`for`、`if`、固定分号、缩进、嵌套和闭合大括号均由模型驱动组合与渲染，玩家只能编辑准备变量、循环初始化/条件/前进、有货判断、交换语句和装载手前进七个槽位。
- 完成：左侧七段技能步骤显式声明对应结构槽位；`for`、`if` 和两个结束作用域标记为锁定结构，页面不再在 JSX 中维护另一份框架关系。
- 完成：Java AST 类型开放给语义分析器；完成长度别名、快指针、慢指针、非零数组条件、循环数据流和两种三行临时变量交换方向的角色识别，进度不再依赖固定变量名或代码行字符串。
- 用户示例：`len / fast / slow / tmp` 七槽组合可生成完整方法体，语义进度为 7/7，并通过全部公开与隐藏用例 8/8。
- 完成：结构组合器同时产出槽位行列范围；空槽先做必填校验，语法和逻辑错误映射并聚焦对应槽位。首次进入及重置保持中性状态，只有运行或提交后才把缺失槽位标红。
- 验证：`npm test -- --run` 通过（8 个测试文件、41 项）；TypeScript 类型检查和生产构建通过；V1 桌面/移动 E2E、V2 双机器人语义回归、代码实战桌面 `1440 x 1000` 与移动 `390 x 844` 全流程均通过，控制台无错误且页面无横向溢出。
- 运行状态：`http://localhost:5173/v2?stage=code` 已启动并返回 HTTP 200。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。
- 下一步：交付用户体验；根据学习过程反馈继续调整槽位提示、字段宽度或错误文案，但不改变已锁定的结构/自由双模式契约。

### 2026-08-24 - `建立会话共享工作记录`（右侧提示去重与变量同步）

- 用户反馈：结构填写右侧槽位的完整答案占位文字与左侧提示重复；答案提示只能保留在左侧，右侧应只显示固定结构、槽位标签和空输入区域。
- 新要求：玩家在右侧使用 `len / fast / slow / tmp` 等自定义变量后，左侧完整逻辑代码应同步改成相同命名，不能继续显示 `writeIndex / scanIndex / temp` 造成对应关系割裂。
- 完成：移除结构填写右侧全部答案型 placeholder；右侧初始和重置状态只保留固定方法、`for / if`、大括号、分号、槽位标签与空输入区域。
- 已纠正：变量角色不再随单行或部分槽位渐进替换。声明区本身不决定快慢指针；只有完整结构通过 AST、作用域和双指针数据流验证后，才从循环、判断、交换和前进中的实际引用识别长度别名、扫描指针、装载指针与临时变量，并一次性更新七段逻辑代码及复制内容。
- 完成：左侧“代码词典”与逻辑参考共用变量角色数据，`装载手 / 扫描手 / 数组访问 / 变量声明 / 比较 / 自增` 等条目会同步为 `slow / fast / tmp`；`nums.length` 作为 Java 数组 API 名称保持不变。
- 验证：结构模式和自由模式的 `len / fast / slow / tmp` 同步、动态复制、无 placeholder、初始/重置中性状态、8/8 用例、双草稿与桌面/移动布局浏览器验收通过；截图目检无答案泄露、遮挡或横向溢出。
- 回归：`npm test -- --run` 通过（8 个测试文件、45 项）；TypeScript、生产构建、V1 桌面/移动 E2E、V2 双机器人语义回归均通过；`http://localhost:5173/v2?stage=code` 返回 HTTP 200。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。
- 下一步：等待用户体验右侧填写与左侧同步的节奏；后续只按实际反馈调整提示呈现，不恢复右侧答案型占位内容。

### 2026-08-24 - `建立会话共享工作记录`（输入空格与角色识别时机修正）

- 用户反馈：结构槽位逐键输入时空格会消失，字符被迫连在一起；此前变量提示在单行输入后立即变化，也错误地把声明顺序当作角色依据。
- Bug 原因：多行槽位渲染时对每行执行 `trim()`，刚输入的行尾空格在下一次按键前已被删除。
- 修正：编辑态不再裁剪行首/行尾空格；组合、解析时仍独立做规范化。浏览器使用逐键输入验证 `int len = nums.length` 能完整保留空格。
- 识别规则：未填完、语法错误、语义错误时不产生新映射；完整 `for -> if -> swap -> slow update` 结构通过安全 Java AST 和双指针数据流验证后，才提交一组新的变量角色。三行声明顺序不参与角色判定。
- 稳定性：已经成功识别的映射在用户编辑下一版但尚未验证完成时保持不变；只有下一份完整正确结构才整体替换，重置才恢复默认命名。自由编写采用相同的完整语义门槛。
- 验证：新增声明乱序、部分结构、完整外形但错误逻辑、自由代码不完整和最后一次有效识别保持测试；结构填写浏览器流程覆盖逐键空格、完成前不替换、完成后整体替换、编辑中不回退。
- 回归：`npm test -- --run` 通过（8 个测试文件、48 项）；TypeScript、生产构建、V1 桌面/移动 E2E、V2 双机器人语义回归、代码实战桌面/移动验收均通过；页面返回 HTTP 200。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-24 - `共创编程逻辑闯关游戏`（锁定结构复核完成）

- 协作完成：当前任务因目录权限保持只读，协调任务在原源码完成实现；双方未复制项目、未更换端口，继续使用同一 `5173` 试玩入口。
- 复核完成：检查 Java AST、角色分析器、七槽组合器、槽位行列映射、翻译轨道关联、双草稿状态、当前模式重置和结构/自由运行路径。
- 复核修正：固定语法、作用域节点和槽位行数已从 JSX 提升为 `STRUCTURED_SCAFFOLD` 共源模型；首次进入和重置后的空结构已从红色错误改为中性“尚未开始填写”，运行或提交后才高亮缺失槽位。
- 视觉复核：桌面和移动截图中，固定语法与可编辑槽位职责清楚；手机端 `for / if / 交换 / 前进` 顺序完整，无重叠和横向溢出。
- 验证确认：协调任务已完成 8 个测试文件、41 项测试、类型检查、生产构建、V1/V2 回归和代码实战桌面/移动全流程；用户自定义命名示例与参考命名均可通过全部 8 个用例。
- 技能记录：`frontend-design` 与 `webapp-testing` 已用于本轮设计和验收；`skill-usage-tracker` 由具备全局统计写权限的协调任务完成记录。
- 下一步：交付用户实际体验，后续只根据学习反馈调整提示密度、字段宽度或错误文案，不改变已确认的锁定结构契约。

### 2026-08-25 - `建立会话共享工作记录`（取消自动变量同步）

- 用户决定：自动调节左侧变量名会显著增加开发与新关卡维护复杂度，不再保留该功能。
- 当前契约：左侧完整逻辑代码、代码词典和复制内容始终使用标准示例 `writeIndex / scanIndex / temp / nums.length`；右侧仍允许玩家使用 `len / fast / slow / tmp` 或其他合法命名。
- 保留：右侧自定义命名继续由现有 AST、作用域和双指针数据流规则判定完成度及运行结果，用户自定义示例仍可达到语义进度 7/7 并通过 8/8。
- 删除：结构模式与自由模式的动态参考识别器、最后一次识别状态、参考变量角色模型、个性化词典生成器及其专项测试全部移除，不再为后续关卡维护提示同步规则。
- 保留修复：结构槽位编辑态不裁剪行尾空格；逐键输入 `int len = nums.length` 可正常保留空格。
- 浏览器确认：右侧显示自定义变量并通过 8/8 时，左侧仍稳定显示标准模板；复制得到标准模板代码；桌面和移动端无回归。
- 回归：`npm test -- --run` 通过（8 个测试文件、41 项）；TypeScript 与生产构建通过；V1 桌面/移动 E2E、V2 双机器人语义回归、代码实战桌面/移动验收均通过。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `建立会话共享工作记录`（算法题通用方法论固化）

- 完成：在技能编程项目根目录新增项目规则文件（现标准名称为 `AGENTS.md`），将后续算法题开发统一为“通用做题引擎 + 每题声明式规格”。
- 方法边界：共享引擎负责参考区、代码词典、结构/自由编辑、草稿、运行、提交、诊断与响应式流程；每道题声明场景、技能、策略、方法签名、scaffold、槽位、参考、映射、语义验证和用例。
- 固化规则：共享页面不得散落题目 ID 或题目代码分支；左侧使用稳定标准参考；右侧接受自定义变量和等价写法；正确性基于 AST、作用域、数据流与真实用例，不恢复自动变量同步。
- 新题流程：依次完成学习目标、世界规则、技能树、题目规格、语义验证、边界用例、共源参考、通用页面接入、浏览器验收和既有版本回归。
- 复杂度约束：至少两题出现相同需求后才提升共享抽象；题目特例留在题目模块；语言能力不足时先扩展解析、诊断和安全执行，不伪装支持。
- Agent MD 分类：以上均依赖技能编程产品，作为项目级规则写入；全局规则文件未修改，项目文件明确引用并遵循现有全局标准。
- 技能记录：本轮已使用并记录 `agent-md-global-rules` 与 `skill-usage-tracker`。

### 2026-08-25 - `建立会话共享工作记录`（默认题目蓝图固化）

- 完成：扩充项目 `AGENTS.md`，将第一题已经验证的展示形式和开发流程整理为后续题目的默认蓝图。
- 默认流程：动手理解 -> 技能认识 -> 规则编排 -> 调试执行 -> 多批验证 -> 代码实战；阶段完成基于真实目标，不以点击、动画结束或固定答案代替。
- 默认展示：技能阶段桌面为场景/执行与技能/规则双工作区，移动端使用适配视图；代码阶段桌面为参考/编辑双工作台，移动端显式切换“参考 / 编辑”。
- 默认交互：原子技能、可重复实例、显式作用域与契约、无状态污染的技能预演、运行中编辑锁定、初始/重置中性反馈、公开用例运行和公开+隐藏用例提交。
- 默认代码学习：左侧零输入即见完整标准参考与原子词典；右侧默认结构填写并保留自由编写；无答案型 placeholder、分模式独立草稿、复制不自动插入、自定义实现不修改标准参考。
- 加速材料：新增下一题题目简报模板，覆盖算法契约、学习目标、世界模型、技能程序、代码实战和验证；新增建议题目模块目录与共享/专用代码边界。
- 边界：固化的是阶段职责、交互形式和验证标准，不是第一题的具体数量；技能数、批次数、槽位数、嵌套深度和用例数仍由每题规格决定。
- Agent MD 分类：以上内容在技能编程后续题目间复用，仍属于项目级产品规则；全局规则未修改。
- 技能记录：本轮已使用并记录 `agent-md-global-rules` 与 `skill-usage-tracker`。

### 2026-08-25 - `项目规则文件标准化命名`

- 完成：将项目根目录的 `agent.md` 重命名为 Codex 自动发现的标准文件名 `AGENTS.md`，正文保持不变。
- 适用范围：从技能编程工作区或其子目录启动的新任务会加载该项目规则，并按规则读取、维护本协作记录。
- 注意：已经运行中的任务不保证自动重新载入改名后的规则文件，需要明确重新读取或新建任务。

### 2026-08-25 - `雨线峡谷` 对齐 AGENTS.md 开始

- 已接受：对照项目 `AGENTS.md` 补齐当前 `http://localhost:5174` 的“接雨水”关卡。
- 差距确认：现有实现已有动手理解、技能预演、规则编排、真实调试和三批验证，但缺少声明式题目规格和最后的 Java 代码实战阶段。
- 实现决定：先把接雨水事实收敛为题目契约，再通过共享式代码工作台消费；代码阶段提供结构填写/自由编写、稳定标准参考、公开/隐藏用例、独立草稿、返回技能调试和安全 Java 子集执行。
- 边界：本轮不改写“移动零”任务的当前状态或产品事实；完成后只对其 V1/V2 做回归验证。
- 验收重点：参考代码与用例共源、自定义变量名可通过语义检查、初始/重置中性、隐藏用例不泄露、桌面/手机无溢出，以及原巡检流程无回归。

### 2026-08-25 - `前端组件复用边界固化`

- 完成：新增项目级 `FRONTEND.md`，明确前端复用目标是共享稳定交互骨架，而不是追求最高复用率。
- 复用门槛：至少两道真实题目需求一致，交互生命周期和状态契约稳定，题目差异可由声明式数据或少量插槽注入，共享 JSX 不判断题目 ID，并且抽取能减少真实行为或测试重复。
- 优先候选：技能编排器、播放控制、多批验证、代码实战工作台、结构编辑器、逻辑参考区；关卡外壳与技能预演仅做轻量或外壳复用。
- 保持专用：完整场景、手动理解任务、世界角色与动画、技能参数、运行帧业务字段、算法语义和题目文案继续留在题目模块。
- AGENTS 入口：涉及关卡前端、共享组件、布局、响应式、技能编排、运行反馈或代码实战时必须先读 `FRONTEND.md`；纯算法或测试改动且不影响界面契约时可不读。
- 分类：以上规则依赖技能编程的关卡模型，全部作为项目级规则维护；全局 Agent 规则未修改。

### 2026-08-25 - `雨线峡谷` 对齐 AGENTS.md 完成

- 题目规格：新增 `src/challenges` 契约与“接雨水”题目模块，统一声明场景、技能、双指针策略、Java 结构、标准参考、词典、公开/隐藏用例与语义验证；`App` 的观察地形、三批验证、技能与标题改为消费该规格。
- 代码实战：新增第三阶段，三批巡检通过后正式解锁，并保留 `?stage=code` 开发验收入口和返回技能调试路径。
- 学习闭环：左侧零输入即显示 10 段稳定标准参考与原子词典，每段可独立复制且不写入编辑器；右侧默认结构填写并保留自由编写，两种草稿独立保存，重置只影响当前模式。
- 语义与执行：新增安全 Java 子集解析/执行和步数上限，支持自定义变量名与等价写法；通过 AST、作用域和数据流强制左右两侧都按“先向内移动 -> 更新岸线 -> 结算新位置”执行，旧的先结算顺序会被拒绝。
- 运行反馈：完成当前公开用例运行、全部公开+隐藏用例提交、隐藏数据保密、结构槽位聚焦、自由代码行定位、初始/重置中性状态与不支持语法说明；同时补上剪贴板权限被拒时的降级处理，避免页面异常。
- 专项验证：“雨线峡谷” 2 个测试文件、16 项单测与生产构建通过；代码实战 Playwright 覆盖结构/自由、自定义变量、10/10 提交、隐藏失败、草稿恢复、重置隔离和桌面/手机；原观察、错误规则、三批验证、五项技能短片回归全部通过。
- 旧题回归：“移动零” 8 个测试文件、41 项单测、TypeScript/生产构建、V1 桌面/手机、V2 技能语义和代码实战验收全部通过。
- 响应式与视觉：桌面 `1440 x 1000` 与手机 `390 x 844` 截图目检无横向溢出、文字/控件重叠或被遮挡，手机端通过“参考 / 编辑”显式切换。
- 复用边界：本轮完整读取 `FRONTEND.md`；未为目录整洁跨仓库搬迁第一题或强行抽取组件。`CodePracticeWorkbench`、`StructuredCodeEditor` 与 `LogicReferencePanel` 的交互契约已在两题验证，后续只在统一源码边界明确时按 Reuse Gate 迁移。
- 运行状态：`http://localhost:5174/` 与 `http://localhost:5174/?stage=code` 保持可用。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `关卡功能与流程规范化` 开始

- 用户指出：“校验全部批次（一键校准）”没有从移动零稳定继承到接雨水，接雨水的巡检程序又预填了初始技能，说明项目缺少可逐题对照的正式功能清单和流程清单。
- 新决定：新增 `CHALLENGE_PLAYBOOK.md` 作为通用功能、六阶段流程、默认状态、题目覆盖矩阵和新题交付清单的事实源；`AGENTS.md` 将其设为相关工作必读文档。
- 默认契约：规则编排必须从空程序开始，空状态中性；正确程序只留在 QA fixture，不提供会向玩家程序注入答案的“恢复初始挑战”。
- 全批校验契约：界面统一使用“校验全部批次”，只验证玩家当前程序，不填充或修复答案；失败可查看首个失败批次，成功后可重新验证，编辑程序必须作废旧验证。
- 复用审查：`BatchVerifier` 已在两题形成等价交互契约，`FRONTEND.md` 从“复用评估”进入“准入待迁移”；当前两套产品源码边界仍分离，本轮不虚构已存在的正式共享源码。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `组件库更新治理完整固化`

- 完成：在 `FRONTEND.md` 增加组件库更新触发点、负责人、用户确认边界、组件生命周期、正式登记表、复用评估队列和自动化启用条件。
- 强制触发：新关卡进入 Definition of Done 前、第二题出现相同交互、正式共享组件契约变化或确认缺陷、组件废弃迁移时，都必须执行对应复用审查或组件库更新。
- 责任人：当前关卡负责 Agent 承担复用审查；直接修改正式共享组件的 Agent 承担使用方识别、迁移、回归和记录。影响学习步骤、场景隐喻、主要交互或已确认视觉体验时必须先取得用户确认。
- 状态边界：组件状态统一为“题目专用 / 复用评估 / 准入待迁移 / 正式共享 / 废弃”；只有存在唯一共享源码并完成多题迁移与回归的“正式共享”组件属于组件库。
- 当前事实：正式共享组件登记表为空；代码实战工作台、结构编辑器和逻辑参考区已通过双题验证，登记为“准入待迁移”；其余跨题骨架继续处于复用评估，仓库与峡谷完整场景保持题目专用。
- 自动化边界：当前尚无统一共享源码目录，因此不得声称自动更新已启用；第一个正式组件晋升时，必须同时建立唯一源码、使用方登记、双题 fixture、统一回归命令及可用时的 CI 触发。
- AGENTS 完成门槛：新关卡未完成复用审查不得交付；修改正式共享组件后，全部登记使用方未迁移并通过回归不得完成任务。
- 分类：本次内容依赖技能编程的关卡与组件治理模型，全部作为项目级规则维护；全局 Agent 规则未修改。
- 技能记录：本轮已使用并记录 `agent-md-global-rules` 与 `skill-usage-tracker`。

### 2026-08-25 - `关卡功能与流程规范化` 完成

- 规范事实源：新增 `CHALLENGE_PLAYBOOK.md`，用带 ID 的功能登记表统一六阶段流程、空程序、编辑失效、逐批验证、全批校验、失败返回、重新验证、代码实战和响应式验收；`AGENTS.md` 已将该清单设为新关卡与通用功能改动的必读和交付门槛。
- 接雨水空态：巡检程序首次进入改为真正的空数组；运行、单步、清空和“校验全部批次”在空态禁用；删除会注入参考技能的“恢复初始挑战”，正确程序只保留在 `qaFixtures.ts`。
- 全批校验：新增当前程序的批次校验器和题目契约字段；失败显示未通过数量、首个具体原因并可进入该段逐帧调试，成功一次解锁代码阶段，重新验证保留程序但清除进度，任何新增、删除或移动技能都会清除旧帧、全部批次结果和代码解锁。
- 算法语义同步：保留并适配六技能顺序“选择较低岸线 -> 低岸向内一步 -> 更新最高柱 -> 计算当前积水”；标准参考为 12 段，结构填写为 10 个语义槽位，技能短片为 6 项。未回退最高柱索引、独立更新帧或显式减法表达。
- 专项验证：接雨水专项单测 `24/24`、全仓测试 `65/65` 和生产构建通过；主流程浏览器验收覆盖空态、技能架拖拽、点选嵌套、错误程序、全批失败查看、修复、全批成功、编辑失效、重新验证、三批逐段执行及手机端最高柱切换；六项技能短片和代码实战结构/自由、公开/隐藏、双草稿、桌面/手机均通过。
- 视觉检查：桌面 `1440 x 1000` 与手机 `390 x 844` 的巡检程序、最高柱状态、积水算式和 10 槽代码结构无横向溢出、遮挡或重叠。
- 旧题回归：移动零 `41/41` 单测和生产构建通过；V1 桌面/手机、V2 技能语义与代码实战专项浏览器验收全部通过。
- 前端复用审查：`BatchVerifier` 已通过两题的等价状态契约复核，继续登记为“准入待迁移”；因两套源码尚未形成唯一共享边界，本轮不虚构正式共享组件，正式组件登记表仍为空。
- 运行状态：`http://localhost:5174/` 与 `http://localhost:5174/?stage=code` 均返回 HTTP 200。

### 2026-08-25 - `项目私有 GitHub 仓库建立`

- 完成：在 GitHub 账号 `Brave-No` 下创建私有仓库 `skill-programming`，地址为 `https://github.com/Brave-No/skill-programming`。
- 初始上传范围：项目协作目录中的 `AGENTS.md`、`FRONTEND.md`、`CHALLENGE_PLAYBOOK.md` 与 `会话协作记录.md`；本次不上传 `node_modules`、构建产物或实际产品源码。
- 本地边界：`/Users/edy/Documents/ChatGPT/技能编程` 作为当前文档版本库；实际产品源码仍位于 `/Users/edy/Documents/Codex/2026-08-20/wo-xi`，后续合并源码前需先明确统一仓库目录结构，避免把两个工作目录直接混为一体。
- 上传前检查：四份项目文档未发现 API key、访问令牌、密码或私钥模式，未发现需删除的临时调试内容。
- 技能记录：本轮已使用并记录 `connect`、`pre-push-cleanup`、`control-chrome` 与 `skill-usage-tracker`。

### 2026-08-25 - `release 分支代码归档`

- 完成：从 `main` 创建 `release` 分支，并将两道真实关卡的源码、测试、配置、依赖锁文件和正式浏览器验收脚本纳入版本库。
- 目录边界：仓库根目录保留“雨线峡谷”应用；“零号仓库”按原有独立 Vite 项目边界归档到 `apps/zero-warehouse`，未在本次上传中强行合并两套运行入口或抽取共享组件。
- 排除项：新增 `.gitignore`，排除所有层级的 `node_modules`、`dist`、`outputs`、覆盖率输出、Python 虚拟环境、系统元数据和 TypeScript 构建缓存。
- 上传前修正：将算法概念契约中的 `skillIds` 从宽泛的 `string[]` 收紧为 `SkillType[]`，修复“雨线峡谷”生产构建中的测试 fixture 类型错误，不改变运行行为。
- 验证：“雨线峡谷”3 个测试文件、24 项测试与生产构建通过；“零号仓库”8 个测试文件、41 项测试与生产构建通过；两套源码的密钥与临时调试内容扫描无异常。
- 技能记录：本轮已使用并记录 `connect`、`pre-push-cleanup` 与 `skill-usage-tracker`。

### 2026-08-25 - `雨线峡谷`（算法概念映射规范与最高柱补齐）

- 用户纠正：问题不只是技能流程与代码步骤数量不一致，而是结果相关变量缺少真实的世界特征、生命周期和跨阶段身份。“左岸最高柱 / 右岸最高柱”之前被藏在“结算”中，导致学习映射不完整。
- 新项目规范：`AGENTS.md` 新增算法概念图方法，要求所有有含义变量、输入输出、公式、控制流、更新顺序、不变量、边界和 API 拥有稳定概念 ID；关键状态需记录初始值、读取点、更新触发、更新规则和结果用途。
- 功能门槛：`CHALLENGE_PLAYBOOK.md` 新增 `mapping.concept-inventory`、`mapping.state-lifecycle`、`mapping.causal-chain` 和 `mapping.stage-parity` 四个必选功能，并把概念覆盖表、公式操作数、状态变化帧和双向追溯加入新题交付清单。
- 题目事实源：接雨水规格新增概念契约，将 `height / left / right / leftMax / rightMax / water`、巡检边界、低岸决策、指针移动、最高柱更新、当前水深、`height.length`、`Math.max` 和返回结果分别建模；技能、追踪帧、参考步骤、词典、槽位与语义检查都已显式关联。
- 关卡修正：新增独立“更新最高柱”技能，巡检因果链固定为“选低岸 -> 向内移动 -> 更新本侧最高柱 -> 最高柱减当前柱高 -> 累加”。执行器保留最高值与来源编号，计算技能不再暗中更新最高柱。
- 场景与代码：地形顶部持续显示左右最高柱高度和来源位置，对应柱显示“左最高 / 右最高”标记；更新最高柱获得独立预演、动画和执行文案。代码实战拆成左右更新槽位与左右积水槽位，参考增至 12 步，词典独立展示左右位置和左右最高柱。
- 自动校验：新增概念链接完整性测试，验证每个概念引用的技能、参考步骤、词典条目、结构槽位和语义检查真实存在；并覆盖缺少最高柱更新、更新时机错误和减法帧文案。
- 验证：当前工作区全部单测通过（12 个测试文件、67 项），TypeScript 与生产构建通过。真实浏览器完成桌面 `1440 x 1000` 与手机 `390 x 844` 验收：最高柱更新到 3 号、完整减法消息、12/12 代码映射、结构填写运行通过，无横向溢出、重叠或控制台错误。
- 复用复核：本次新增的概念规格是通用题目契约；峡谷最高柱读数、柱标记与预演仍为接雨水专用表现，未强行晋升共享前端组件，`FRONTEND.md` 无需新增组件登记。
- Agent MD 分类：映射方法依赖技能编程的关卡阶段和题目契约，全部作为项目级规则固化；全局规则文件未修改。
- 技能记录：本轮已使用并记录 `agent-md-global-rules`、`frontend-design`、`webapp-testing`、`browser:control-in-app-browser` 与 `skill-usage-tracker`。

### 2026-08-25 - `技能编程完整软件入口与关卡选择`

- 用户决定：已完成 SOP 验证的游戏进入同一个完整软件，玩家先在挑战目录选择已设计好的游戏，再直接进入对应挑战。
- 源码边界：当前活跃产品源码正式收敛到 `/Users/edy/Documents/ChatGPT/技能编程`；根目录保留“雨线峡谷”题目模块，“零号仓库”以原行为迁入 `apps/zero-warehouse`。旧目录 `/Users/edy/Documents/Codex/2026-08-20/wo-xi` 不再承载统一入口。
- 产品入口：新增声明式 `src/product/challenges.ts` 和根路径挑战目录；目录当前登记“零号仓库 / 移动零”与“雨线峡谷 / 接雨水”，展示真实运行截图、策略、六阶段与 Java 信息。
- 路由契约：`/` 为挑战选择，`/games/zero-warehouse/` 与 `/games/rainline/` 分别加载独立关卡页面；两题仍隔离自己的样式和场景实现，统一构建一次生成三个 HTML 入口。
- 返回与记忆：两题普通阶段和代码阶段均新增“挑战选择”返回命令，同时保留代码实战返回技能调试；目录只用本地键记录“上次进入”，不伪装成阶段完成或通关进度。
- 前端复用审查：目录清单与产品入口形成唯一源码；两题返回入口因页头结构和视觉不同登记为 `ChallengeCatalogNavigation` 复用评估，未虚构正式共享 JSX，正式共享组件登记表仍为空。
- 工程验证：统一仓库 12 个测试文件、67 项通过，移动零 8 个测试文件、41 项通过；统一多页面生产构建与移动零独立生产构建均通过。
- 浏览器验证：产品目录桌面 `1440 x 1000`、手机 `390 x 844` 的真实预览、两题选择、上次进入、普通阶段返回、代码阶段返回、图片加载、无横向溢出和控制台无错误均通过；雨线主流程、六项技能预演和代码实战通过；移动零 V1、V2 与代码实战专项全部通过。
- 运行状态：统一试玩服务为 `http://localhost:5174/`；当前根目录、两个关卡入口和预览资源均返回 HTTP 200。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `代码提交首个失败反例`

- 用户要求：全部用例提交未通过时，不能只显示通过数量或“隐藏用例失败”；即使多个用例失败，也只需展示按执行顺序卡住的第一个用例，并让玩家看清该示例。
- 结果状态：代码实战提交结果统一保存 `firstFailure`，不再区分“首个公开失败”和只有布尔值的隐藏失败。提交新结果会清除旧的单次运行反馈，成功提交不会残留旧反例。
- 失败反馈：新增“当前卡住的用例”，展示用例序号、公开/隐藏属性、输入、期望结果、实际结果和具体原因；运行未产生返回值时明确显示“未产生返回值”。多个失败用例不会展开后续内容。
- 隐藏边界：隐藏用例在提交前保持不可见；提交后仅当某个隐藏用例成为首个失败反例时揭示这一条，其他隐藏用例继续不展示。
- 通用规范：`AGENTS.md` 与 `CHALLENGE_PLAYBOOK.md` 已同步首个失败反例契约，并将多失败单例展示和移动端无横向溢出纳入新关卡交付检查。
- 验证：全仓 12 个测试文件、67 项单测和生产构建通过；真实浏览器用一段“3 个公开用例通过、7 个隐藏用例失败”的代码确认只展示第 4 个用例，内容为输入 `[]`、期望 `0`、实际 `1` 和失败原因，未出现后续 `[2]` 用例。桌面 `1440 x 1000` 与手机 `390 x 844` 均无横向溢出或控制台错误。
- 技能记录：本轮已使用并记录 `frontend-design`、`agent-md-global-rules`、`browser:control-in-app-browser` 与 `skill-usage-tracker`。

### 2026-08-25 - `挑战目录 V2 扩展契约确认`

- 用户确认：在继续开发新题前，先把统一软件目录的扩展方式正式固化；提前建立分类、筛选、状态和题量增长契约，但不立即实现没有真实题量支撑的复杂控件。
- 目录元数据：从下一道新题开始按 `ChallengeCatalogEntryV2` 声明学习专题、数据结构、解题技巧、`intro / intermediate / challenge` 学习难度、开放状态、目标语言、阶段数、预计时长、前置关卡，以及显式预览/路径状态；`available` 必须有真实游戏预览和可达路径，非开放状态不得使用虚构截图或无效链接。现有两题在 Catalog V2 实现时迁移。
- 分类决定：学习专题/路径作为一级组织，数据结构、技巧、难度和状态作为筛选维度，避免把“双指针”和“数组”等不同维度混成单一分类。
- 题量演进：1-4 题保留真实预览精选卡片；5-12 题增加专题分组和基础筛选；13 题及以上增加搜索、排序和真实继续学习入口。分页或虚拟化只在真实性能和浏览成本需要时引入。
- 状态边界：开放、开发中和锁定必须反映真实产品状态；锁定需有明确前置条件。“上次进入”不等于学习进度，继续学习和通关状态必须等待真实持久化事实。
- 文档完成：`AGENTS.md` 新增统一软件与 Product Catalog Contract；`CHALLENGE_PLAYBOOK.md` 新增目录 V2 状态、功能 ID、产品覆盖和交付门槛；`FRONTEND.md` 新增信息架构、题量密度、桌面/移动交互和当前实现边界。
- 当前实现边界：本轮只固化产品契约，没有修改目录源码或现有页面；下一道新题开发前先迁移两题目录数据并清除固定题量、固定专题和固定开放状态假设。
- Agent MD 分类：以上均依赖技能编程的关卡目录、学习路径和产品状态，属于项目级规则；全局规则文件未修改。
- 技能记录：本轮已使用并记录 `frontend-design`、`agent-md-global-rules` 与 `skill-usage-tracker`。

### 2026-08-25 - `工作线程2`（项目熟悉与基线核对）

- 任务定位：当前 Codex 任务已命名为“工作线程2”；本轮只熟悉项目文档、源码边界和验证基线，等待用户分配具体题目开发。
- 已完整读取：全局 Agent 规则、`AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md`、`README.md` 与本协作记录；后续新题按统一目录、六阶段流程、算法概念图、声明式题目规格和前端复用审查执行。
- 源码认识：根目录是统一挑战目录与“雨线峡谷”活跃应用，`apps/zero-warehouse` 保留“零号仓库”独立应用；统一构建包含目录、`/games/rainline/` 和 `/games/zero-warehouse/` 三个入口。
- 当前边界：雨线峡谷已具有 `AlgorithmChallenge`、概念映射、语义验证与代码实战规格；零号仓库 V2 仍保留独立但已验证的对应实现。正式共享组件登记表为空，批次验证与代码实战相关候选处于准入待迁移或复用评估状态。
- 目录前置：Catalog V2 当前只有文档契约，源码仍是两题固定旧字段；下一道新题开发前需先迁移现有两题元数据，并移除固定题量、固定专题和固定开放状态假设。
- 工作区保护：`release` 分支已有多项未提交产品变更；本线程将其视为现有协作成果，不清理、不覆盖。本轮除追加本条协作事实外未修改业务源码。
- 基线验证：`npm run test:all` 通过，统一仓库 12 个测试文件、67 项，零号仓库 8 个测试文件、41 项；`npm run build:all` 通过，两套生产构建均成功，保留现有包体积提示。
- 下一步：等待用户分配具体算法题；接题后先补全题目简报与概念覆盖表，再按功能 ID、目录契约、浏览器验收和既有题回归完成交付。

### 2026-08-25 - `工作线程3`（项目熟悉与基线核对）

- 任务定位：当前 Codex 任务已命名为“工作线程3”；本轮只熟悉项目文档、源码边界和验证基线，等待用户分配具体题目开发。
- 已完整读取：全局 Agent 规则、`AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md`、`README.md` 与本协作记录；后续新题按统一目录、六阶段流程、算法概念图、声明式题目规格和前端复用审查执行。
- 源码认识：根目录提供统一挑战目录和“雨线峡谷”，`apps/zero-warehouse` 保留“零号仓库”独立应用；统一生产构建包含目录、`/games/rainline/` 和 `/games/zero-warehouse/` 三个入口。
- 当前边界：“雨线峡谷”已落地 `AlgorithmChallenge`、概念映射、语义验证和代码实战规格；“零号仓库”V2 仍保留独立但已验证的同类实现。正式共享组件登记表为空，批次验证和代码实战候选仍处于准入待迁移或复用评估状态。
- 目录前置：Catalog V2 当前只有已确认的文档契约，源码仍使用两题旧版固定字段；下一道新题开发前需先迁移现有两题元数据，并移除固定题量、固定专题和固定开放状态假设。
- 工作区保护：`release` 分支已有多项未提交协作成果；本线程不清理、不覆盖。本轮除追加本条协作事实外未修改业务源码。
- 基线验证：`npm run test:all` 通过，统一仓库 12 个测试文件、67 项，零号仓库 8 个测试文件、41 项；`npm run build:all` 通过，两套生产构建均成功，保留现有包体积提示。
- 下一步：等待用户分配具体算法题；接题后先补齐题目简报、概念覆盖表和 Catalog V2 接入前置，再按功能 ID、浏览器验收、复用审查和既有题回归完成交付。

### 2026-08-25 - `工作线程2`（和为 K 的子数组开始）

- 已接受：开发 LeetCode 560“和为 K 的子数组”完整新关卡，并接入统一挑战目录与玩家流程。
- 世界方向：采用“前缀和档案站”；数组元素表示逐站数值变化，累计刻度对应 `prefixSum`，目标旧刻度对应 `prefixSum - k`，档案柜对应前缀和频次表，命中数量累加为答案。
- 核心因果链：初始化 `0 -> 1` 档案，逐项累加当前值，计算目标旧刻度，先查询并累加历史命中，再登记当前刻度；使用 `k = 0` 与负数批次验证查询/登记顺序和重复前缀频次。
- 工程前置：本题需要先实现 Catalog V2 元数据迁移；Java 安全子集需显式增加受控 `Map<Integer, Integer>`、`HashMap`、`put` 与 `getOrDefault` 能力，不调用系统 JDK、不使用 `eval`。
- 交付范围：题目简报与概念覆盖、声明式规格、技能程序、真实执行帧、批次验证、结构/自由代码实战、语义检查、独立路由、真实预览、桌面/手机浏览器验收、既有两题回归及前端复用审查。
- 技能记录：本轮已使用并记录 `frontend-design` 与 `webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。
- 下一步：完成题目简报与技术契约后开始实现题目内核和 Catalog V2。

### 2026-08-25 - `工作线程3`（和为 K 的子数组开发开始）

- 已接受：将 LeetCode 560“和为 K 的子数组”作为第三道正式关卡接入统一产品，完成目录、独立路由、六阶段学习闭环、Java 代码实战和旧题回归。
- 算法决定：学习策略使用“前缀和 + 频次表”，明确覆盖负数输入，不采用依赖窗口单调性的滑动窗口。
- 世界模型（计划）：玩家沿数据轨道前进，持续观察当前前缀和、目标 `k`、所需历史前缀 `prefix - k`、该前缀的历史出现次数和累计答案；因果链为“累加当前值 -> 计算所需旧刻度 -> 查询次数 -> 累加答案 -> 登记当前前缀”。
- 语言前置（计划）：扩展受限 Java 子集，显式支持本题所需的 `Map<Integer, Integer>`、`new HashMap<>()`、`getOrDefault` 和 `put`；不伪装支持完整 Java 集合框架。
- 目录前置（计划）：先把现有两题迁移到 Catalog V2 元数据契约，再登记第三题，继续使用 1-4 题真实预览精选布局，不提前展示筛选和搜索。
- 技能记录：本轮已读取并记录 `frontend-design`、`webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。
- 下一步：完成题目简报、概念覆盖、Catalog V2 数据迁移与语言子集扩展。

### 2026-08-25 - `工作线程0`（无重复字符的最长子串开发开始）

- 已接受：将 LeetCode 3“无重复字符的最长子串”作为正式新关卡接入统一产品，完成目录、独立路由、六阶段学习闭环、Java 代码实战和既有题回归。
- 算法决定：采用“滑动窗口 + ASCII 字符频次数组”；右侧逐字扫描，发现重复时左侧逐项移出，恢复窗口唯一性后纳入当前字符、更新最长记录并继续扫描。
- 世界模型：使用“字符灯廊”表达连续窗口；左右边界、当前字符、窗口字符频次和历史最长区间持续可见，重复字符触发警报，缩窗过程逐帧显示移出字符及频次变化。
- 语言前置：扩展受限 Java 子集，仅增加本题所需的字符串入参、`String.length()`、`String.charAt()` 与 `new int[...]`；继续保持安全解释执行、步数上限和明确的不支持反馈。
- 共享边界：计划把无参数嵌套技能编排骨架迁入唯一共享源码供“接雨水”和本题共用，并把现有代码工作台的草稿键、输入展示、结果单位和语义槽位定位改为题目规格驱动；“零号仓库”的带参数能力契约仍保持专用实现。
- 并行工作保护：已发现工作线程2/3正在开发“和为 K 的子数组”并可能修改 Catalog V2 与 Java 子集；本线程先在独立题目目录实现，触及共享文件前重新读取最新内容并合并，不覆盖其他线程成果。
- 技能记录：本轮已读取并记录 `frontend-design`、`webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。
- 下一步：完成题目简报与独立题目内核，再接入最新 Catalog V2 和共享前端契约。

### 2026-08-25 - `工作线程1`（找到字符串中所有字母异位词开发开始）

- 已接受：将 LeetCode 438“找到字符串中所有字母异位词”作为正式新关卡接入统一产品，完成目录、独立路由、六阶段学习闭环、Java 代码实战和既有题回归。
- 算法决定：采用“固定宽度滑动窗口 + 26 格字母频次”；右侧逐字纳入，窗口超宽时先移出左侧字母再推进左边界，固定宽度且目标/窗口频谱相等时记录左边界。
- 世界模型：“频谱滑窗站”。源字符串是一条字母信号带，目标串是一张目标频谱卡，左右夹具围成当前窗口，两排 26 格频谱持续显示进入、离开与匹配变化。
- 语言前置：扩展受限 Java 能力以支持字符串、字符读取、固定数组、`for`、`List<Integer>`、`ArrayList`、`Arrays.equals` 与 `List.add`；继续保持安全解释执行、步数上限和明确的不支持反馈。
- 并行工作保护：已发现工作线程0、2、3也在新增关卡并计划修改 Catalog V2/Java 子集；本线程先在 `src/challenges/find-all-anagrams` 独立实现，触及共享文件前重新读取并合并最新成果，不覆盖其他线程修改。
- 题目简报：已新增 `src/challenges/find-all-anagrams/BRIEF.md`，固定算法契约、19 个概念节点、10 项原子技能、结构槽位、真实用例和功能 ID 覆盖。
- 技能记录：本轮已读取并记录 `frontend-design`、`webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。
- 下一步：先完成独立题目内核、解释器、语义与测试，再接入届时最新的 Catalog V2 和共享前端契约。

### 2026-08-25 - `通用通关动画正式确认与下发`

- 用户确认：采用当前 `http://localhost:5174/?celebration=preview` 的“算法回路闭合”版本作为所有算法关卡的通用通关反馈，不再继续修改本轮视觉方向。
- 功能登记：`CHALLENGE_PLAYBOOK.md` 新增必选功能 `feedback.completion-celebration`；只有代码实战首次真实通过全部公开与隐藏用例时自动展示，动画不参与通关判定。
- 稳定契约：固定表达六阶段学习旅程、算法核心闭合和真实通过数量；支持重播、关闭、`Esc` 跳过和系统减少动态效果，关闭后保留代码、草稿、测试结果和通关结论，不自动跳转。
- 复用边界：`ChallengeCompletion` 进入 `FRONTEND.md` 复用评估队列。当前源码仍只服务产品目录预览，尚未在两道真实关卡完成迁移与回归，因此正式共享组件登记表继续为空。
- 覆盖状态：移动零 V2 与接雨水均标记为“待同步”；由任务 `01a037e5-c5c4-71e0-902d-1f5512760daa`（通用功能同步与回归）负责识别全部真实使用方、接入、桌面/手机/减少动态效果验收和覆盖矩阵写回。
- 在途关卡：已经启动但尚未完成的新关卡任务必须重新读取最新功能契约，直接按该功能 ID 接入，不等待成为旧项目后再补丁迁移。
- Agent MD 分类：该通关表达依赖技能编程的六阶段流程、真实代码提交和产品视觉，属于项目级规则；全局规则文件未修改。
- 技能记录：本轮已使用并记录 `agent-md-global-rules` 与 `skill-usage-tracker`。

### 2026-08-25 - `feedback.completion-celebration` 正式共享与双题回归

- 并行职责收敛：“和为 K 的子数组”题目模块、UI、路由、Catalog 与验收由工作线程2继续负责；工作线程3停止重复页面开发，删除了重复的 `src/challenges/subarray-sum-equals-k/brief.md`，且未覆盖 `src/challenges/subarray-sum-k/`。
- 共享题目契约：`AlgorithmChallenge` 现支持 `ProgramAst / TInput / TOutput / TSkill / TBatch / TScene` 题目泛型，`TBatch extends VerificationBatchBase` 允许题目声明自有验证批次字段；`codePractice` 通过 `runtime.parse/run/formatInput/formatOutput`、`presentation`、`checkToSlot` 和可选 `draftStorage.legacyKeys` 提供题目事实。
- Java 安全子集：支持受控 `Map<Integer, Integer>`、`new HashMap<>()`、`getOrDefault`、`put` 与命名方法入参；未支持集合 API 继续返回明确 `unsupported`，循环继续受步数上限保护。
- 通用代码实战：`src/components/CodePractice.tsx` 不再直接绑定雨线运行器、输入展示、结果单位或语义检查到槽位的映射；自由/结构/模式键统一按 `challenge.id` 隔离，雨线旧 `rainline:*` 草稿可一次性读取并写入新键。
- 通关触发：雨线和移动零都直接引用唯一 `src/shared/completion/ChallengeCompletion.tsx`；真实全用例通过时先持久化完成记录并提交结果，再展示动画。失败、不完整、重复提交和刷新均不自动重播；关闭后提交结果、代码和草稿保留。
- 正式共享登记：`FRONTEND.md` 已将 `ChallengeCompletion` 登记为首个正式共享组件，公开导出仅消费 `passedCount`、`total`、`onClose`；本地登记门禁 `node work/verify_shared_registry.mjs` 通过，确认唯一源码与两个真实使用方。
- 验证结果：共享完成组件与 Java/代码实战专项 `12/12` 通过，子数组联合专项 `19/19` 通过，零号仓库 `41/41` 通过；目录显式预览的桌面/手机/重播/关闭/`Esc`/减少动态验收通过。雨线真实代码提交另验证了旧草稿迁移、关卡键隔离、首次 `10/10` 触发、完成记录、重复提交/刷新不重播、结果保留和手机减少动态无溢出。
- 全量门禁结果：并行题目的在途类型问题合并修复后，`npm run verify:shared` 最终通过：正式共享登记检查通过，共享 fixture `2/2`、根应用 `122/122`、零号仓库 `41/41` 通过，TypeScript 与两套生产构建成功；仅保留现有的主包体积提醒。
- 双题浏览器验收：雨线峡谷桌面 `1440 x 1000` 和手机 `390 x 844` 验证首次 `10 / 10`；零号仓库 V2 同视口验证首次 `8 / 8`。两题均覆盖不完整/失败不触发、真实计数、重播、关闭按钮、结果按钮、`Esc`、关闭后结果保留、重复提交、刷新不重播、控制台无错误和无横向溢出；减少动态效果下动画名为 `none`、完整终态可读，视口截图确认两题只存在计数差异。
- 既有流程回归：接雨水动手理解、错误规则修复、三批验证、六项技能预演和桌面/手机流程通过；移动零 V2 技能语义、运行标记和手机布局通过；移动零 V1 桌面完整流程与手机触控布局通过，未新增代码实战通关入口。
- 在途任务通知：已向 `工作线程0`（`01a037f2-0f65-72c1-9da5-9a5dec4efd3e`）、`工作线程1`（`01a037f3-a444-7521-9a02-0be08ff0e7d7`）、`工作线程2`（`01a037f4-eeab-7931-a03e-348466e1021f`）、`工作线程3`（`01a037f4-5d5d-7a32-893e-e8f027a6e4a9`）和 `工作线程4`（`01a037f5-ada6-7a20-837a-1ecdcd119b6a`）发送最新契约，要求首次真实全部用例通过时直接引用正式共享组件，不改动其在途题目代码。
- 独立待办边界：基线审查中的 `code.run-submit` 漂移和移动零概念映射待复核仍保持独立，本次未混入无关修复。
- 技能记录：本轮已使用并记录 `frontend-design`、`webapp-testing` 与 `skill-usage-tracker`。

### 2026-08-25 - `工作线程2`（和为 K 的子数组完成）

- 关卡闭环：完成“前缀和档案站 / 和为 K 的子数组”六阶段关卡。题目事实收敛在 `src/challenges/subarray-sum-k/`，覆盖题目简报、概念图、七个原子技能、能力与作用域契约、真实执行帧、三批自动验证、标准/改名/错误实现和 Java AST 语义检查。
- 算法映射：世界规则固定为“初始化 `0 -> 1` 档案 -> 累加前缀 -> 计算 `prefixSum - k` -> 查询并累加历史次数 -> 登记当前前缀 -> 探针前进”；前缀和、目标旧刻度、频次表和答案的初始化、读取、更新顺序均可在场景、技能、帧、参考、词典、结构槽位和语义检查间追溯。
- 代码实战：正式共享 `CodePracticeWorkbench` 已消费题目声明的对象输入 `{ nums, k }`、运行时格式化、结构 scaffold、参考步骤、原子映射、公开/隐藏用例与槽位诊断；受限 Java 运行时支持本题所需的 `Map<Integer, Integer>`、`HashMap`、`getOrDefault` 和 `put`，继续保持步数上限与明确的不支持反馈。
- 通关反馈：本题通过共享代码工作台直接使用唯一 `src/shared/completion/ChallengeCompletion.tsx`；首次真实通过全部 `9/9` 用例时先持久化完成事实和提交结果，再展示动画。失败、不完整、重复提交和刷新均不会循环触发，关闭动画不清除代码、草稿或结果。
- 产品接入：新增 `/games/subarray-sum-k/` 独立入口、统一 Vite 构建入口、Catalog V2 开放条目与真实预览 `public/previews/subarray-sum-k.png`；目录卡片可达、图片真实加载，关卡页保留挑战目录和技能调试返回路径。
- 前端复用审查：`FRONTEND.md` 已将本题登记为 `ChallengeCompletion` 使用关卡，并将 `CodePracticeWorkbench` 晋升为正式共享组件；结构编辑器与逻辑参考区继续作为工作台内部复用能力，不复制题目专用版本。`CHALLENGE_PLAYBOOK.md` 已同步本题功能覆盖与当前目录状态。
- 题目专项：本题与共享运行时专项共 `25/25` 通过。完整浏览器验收覆盖桌面和手机的手动错误/修正、技能预演不污染程序、鼠标拖放与手机点选、空程序与错误程序、失败批次查看、七技能正确编排、三批验证、结构填写、当前用例、全部提交、首次通关、重复提交、刷新恢复、目录预览以及无横向溢出。
- 最终门禁：`npm run verify:shared` 通过，正式共享登记检查、共享组件 `2/2`、根仓 `122/122`、零号仓库 `41/41`、全局 TypeScript 和两套生产构建全部成功；`dist/games/subarray-sum-k/index.html` 已生成，仅保留既有主包体积提醒。
- 运行状态：统一目录为 `http://127.0.0.1:5182/`，本关为 `http://127.0.0.1:5182/games/subarray-sum-k/`，当前均返回 HTTP 200。

### 2026-08-25 - `代码实战多语言支持开始`

- 用户确认：代码实战阶段从仅 Java 扩展为 C++、Python、JavaScript、Java 四种可选语言。
- 实现边界：算法场景、技能、概念 ID、语义检查和用例保持同源；方法签名、完整参考、代码词典、结构 scaffold、编辑器高亮、解析诊断、安全执行与草稿存储按语言切换。
- 安全决定：不使用浏览器 `eval`、系统编译器或不受控执行；各语言代码先通过独立受限语法适配归一到现有安全 AST，再复用题目语义验证与真实用例执行。超出支持范围继续明确报错。
- 共享职责：优先修改正式共享 `CodePracticeWorkbench`，识别并回归其全部登记使用方；移动零 V2 仍是独立旧页面，需要接入同一语言定义和适配规则，不能只改其中一道题。
- 草稿规则：按“题目 ID × 语言 × 结构/自由模式”隔离；切换语言不翻译或覆盖玩家已有草稿，切回时恢复原语言内容。通关事实仍属于题目，不按语言重复计算。

### 2026-08-25 - `工作线程4`（最小覆盖子串完成）

- 关卡闭环：完成“窗口校准台 / 最小覆盖子串”六阶段关卡，题目事实收敛在 `src/challenges/minimum-window-substring/`。世界规则使用源文字带、目标需求卡、字符欠账台、总欠账、左右标尺与历史最短封条，19 项技能逐步表达目标登记、窗口扩张和覆盖收缩的完整因果链。
- 算法与验证：标准解法为固定 `int[128]` 欠账表、外层扩张与内层收缩；受限 Java 增量支持字符串返回与 `substring(start, end)`。标准实现和变量改名/比较反向等价实现通过全部用例，重复字符只记种类、错误更新顺序和非法子串区间会被语义或运行层拒绝。
- 代码实战：题目直接复用正式共享 `CodePracticeWorkbench`，消费字符串对象输入、题目 presentation、24 项结构槽位、29 段完整参考、独立 API 词典、槽位诊断和 10 个公开/隐藏用例；结构填写与自由编写均在真实浏览器完成提交。
- 通关反馈：首次真实 `10/10` 通过时，共享工作台先写入 `skill-programming:minimum-window-substring:completion:v1`，再展示唯一 `ChallengeCompletion`；重复提交和刷新不自动重播，关闭后保留代码与提交结果。未复制或修改通关组件源码。
- 产品接入：新增 `/games/minimum-window/` 独立入口、Vite 生产构建入口、Catalog V2 开放条目与真实预览 `public/previews/minimum-window.png`。当前目录已合并六道开放题，并进入 5-12 题资料库层；本题浏览器验收覆盖三个专题、组合筛选、空态和桌面/手机无溢出。
- 前端复用审查：正式登记表新增“窗口校准台”为 `ChallengeCompletion` 和 `CodePracticeWorkbench` 使用方。字符带/欠账台、19 节点严格因果编排器与技能预演保持题目专用；`ChallengeShell`、`SkillProgramBuilder`、`PlaybackControls` 和 `BatchVerifier` 继续按队列评估，不新增题目 ID 分支或复制共享 JSX。
- 浏览器验收：`work/e2e_minimum_window.mjs` 在桌面 `1440 x 1000` 与手机 `390 x 844` 通过，覆盖手动错误/修正、无污染预演、空程序、桌面拖放、19 节点嵌套编排、播放/暂停/单步/重置、三批校验、重新验证锁定、结构/自由代码、首次通关、重复提交、刷新恢复、目录筛选、真实预览、控制台无错误和无横向溢出。
- 自动回归：本题专项 `13/13` 通过；`npm run verify:shared` 通过正式共享登记、共享组件 `4/4`、根仓 `128/128`、零号仓库 `41/41`、TypeScript 和七入口生产构建，仅保留现有主包体积提醒。
- 并行协作：目录验收发现另外两道在途关卡的预览尚未全部落盘，已通知对应线程；“字符灯廊”随后补齐 `public/previews/character-corridor.png`，本题预览与路由不受影响。
- 技能记录：本轮使用并记录 `frontend-design` 与 `webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。

### 2026-08-25 - 公网发布改为任务完成通知触发

- 用户修正：不再使用定时或心跳监控；下方“公网新题自动发布监控启用”是已废止的历史记录，其每 10 分钟自动化及完整验证门禁均已被本节决定替代，相关自动化已删除。
- 触发方式：相关开发任务在当前任务真正完成时，主动向“部署服务”任务 `01a0380a-f6ad-7830-bf3c-5825bc77e36d` 发送以“部署通知：”开头的完成消息；任务中途暂停不得发送可部署通知。
- 通知内容：完成任务名称、完成内容、新增或变化路由、已做/未做验证、已知问题和“当前工作区可部署”。当前工作线程 0-4、语言通用、彩蛋开发与彩蛋开发同步助手均已收到该协作约定。
- 发布门槛：允许部署尚未经过完整测试或浏览器验收的完成成果；部署任务应如实保留验证状态，不再因缺少完整验证本身拒绝发布。源码至少需要能够产出可上传的静态文件，不能构建时应报告阻塞，不修改业务代码冒充完成。
- 发布安全：继续使用版本目录、校验上传包、原子切换并保留上一线上版本；发布后的公网可达性检查和失败回滚仍保留，因为这些是服务器发布安全措施，不属于题目产品验证。

### 2026-08-25 - 公网新题自动发布监控启用（已废止）

- 历史自动化：曾在当前“部署服务”任务启用心跳监控“监控新题并自动部署”，每 10 分钟检查一次工作区；该自动化已于同日按用户新决定删除，不再运行。
- 发布门禁：仅当 `npm run test:all`、`npm run build:all`、Catalog 全部 `available` 路由、真实预览和对应构建产物均通过，且构建前后源码指纹一致时，才允许发布到 `39.96.62.70`。
- 并行保护：监控只部署，不修改题目、测试、配置或产品代码；检测到测试/构建失败、产物缺失或构建期间源码变化时放弃本轮，保留现有线上版本。
- 发布策略：使用时间戳版本目录、上传包 SHA-256 校验和 `current` 原子切换；切换后同时验证服务器本机与公网目录、全部开放关卡和预览，失败时立即回滚到上一版本，不自动删除旧版本。
- 凭据约束：服务器凭据只从本地 `server-credentials.env` 读取，不得输出到消息、日志或协作记录。
- 首轮检查：根应用 `128/128`、零号仓库 `41/41` 测试和两套生产构建均通过，但构建期间“最小覆盖子串”“字符灯廊”和“字母异位词”相关文件仍在写入，源码指纹发生变化，因此按门禁取消本轮发布；公网目录、零号仓库和雨线峡谷继续返回 HTTP 200，线上稳定版本未受影响。

### 2026-08-25 - `工作线程1`（找到字符串中所有字母异位词完成）

- 关卡闭环：完成“频谱滑窗站 / 找到字符串中所有字母异位词”六阶段关卡；题目事实收敛在 `src/challenges/find-all-anagrams/`，覆盖题目简报、19 个概念节点、10 项原子技能、手动窗口选择、三批验证、标准/改名/典型错误实现、Java AST 语义与安全解释执行。
- 算法映射：固定因果链为“构建目标 26 格频谱 -> 右侧字母入窗 -> 超宽时先移出旧字母再推进 left -> `Arrays.equals` 比较 -> `List.add(left)` 记录”；目标/窗口频谱、左右边界、字符偏移、结果列表和字符串/List API 均可在场景、技能、帧、参考、词典、15 项结构槽位和语义检查间追溯。
- 代码实战：正式共享 `CodePracticeWorkbench` 消费 `{ source, pattern }` 输入、数组/List 输出格式、题目 scaffold、14 段参考、24 项原子映射和 9 个公开/隐藏用例；Java 子集显式支持 `for`、字符字面量、`new int[26]`、`String.length/charAt`、`List<Integer>`、`ArrayList`、`Arrays.equals` 与 `List.add`，未支持能力继续明确拒绝。
- 通关反馈：本题不复制或修改组件，直接经共享工作台使用唯一 `ChallengeCompletion`。首次真实 `9/9` 通过时先写入 `skill-programming:find-all-anagrams:completion:v1`，再展示动画；浏览器已验证重播、`Esc`、关闭后结果保留、重复提交与刷新不自动重播，以及手机完整稳定终态。
- 产品接入：新增 `/games/find-all-anagrams/` 独立入口、Vite 生产入口、Catalog V2 开放条目和真实预览 `public/previews/find-all-anagrams.png`；当前六题目录的三个专题、五类筛选、组合空态、图片加载和桌面/手机无横向溢出均通过。
- 前端复用审查：`SkillProgramBuilder` 已形成唯一共享源码 `src/shared/program/SkillProgramBuilder.tsx`，接雨水通过薄适配与本题直接引用；双题真实回归覆盖空态、鼠标拖放、点选、嵌套、重排、删除、编辑失效和无溢出，因此从复用评估晋升为正式共享。字符灯廊所需的多类嵌套作用域与移回技能架仍由题目专用 `WindowProgramBuilder` 承担；源信号带、双 26 格频谱和技能预演继续保持题目专用。
- 自动与浏览器验证：本题套件与共享编排器新增 `5/5` 回归；最终 `npm run verify:shared` 通过正式登记检查、共享组件 `4/4`、根仓 `128/128`、零号仓库 `41/41`、全局 TypeScript 与两套生产构建，产物包含 `dist/games/find-all-anagrams/index.html`。`work/e2e_find_all_anagrams.mjs` 在桌面 `1440 x 1000`、手机 `390 x 844` 通过本题完整流程、隐藏反例、目录与接雨水共享编排器回归，控制台无错误。
- 技能记录：本轮使用并记录 `frontend-design` 与 `webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。

### 2026-08-25 - `工作线程0`（无重复字符的最长子串完成）

- 关卡闭环：完成“字符灯廊 / 无重复字符的最长子串”六阶段关卡。题目事实收敛在 `src/challenges/longest-substring-without-repeating-characters/`，覆盖题目简报、概念图、九个原子技能、双层作用域、真实运行帧、三批自动验证、标准/变量改名/隐藏边界错误实现和 Java AST 语义检查。
- 算法映射：世界规则固定为“初始化窗口 -> 右侧读取字符 -> 重复时逐项收缩左侧 -> 纳入当前字符 -> 用 `right - left + 1` 更新最长值 -> 右侧前进”；左右边界、当前字符、频次数组、唯一窗口不变量和历史最长值的生命周期均可在场景、技能、帧、参考、词典、结构槽位和语义检查间追溯。
- Java 与代码实战：受限 Java 运行时支持字符串入参、`String.length()`、`charAt()`、`new int[...]` 和块级作用域；正式共享 `CodePracticeWorkbench` 消费题目 runtime、presentation、结构 scaffold、参考、映射、9 个公开/隐藏用例和槽位诊断，接受变量改名与等价比较方向，不依赖标准代码字符串。
- 通关反馈：本题通过共享代码工作台直接引用唯一 `src/shared/completion/ChallengeCompletion.tsx`。首次真实 `9/9` 通过时先持久化完成事实和提交结果，再展示动画；失败、不完整、重复提交和刷新均不触发或循环播放，关闭与 `Esc` 后保留代码、草稿和结果，减少动态效果直接呈现稳定终态。
- 产品接入：新增 `/games/character-corridor/` 独立入口、Vite 生产入口、Catalog V2 开放条目和真实预览 `public/previews/character-corridor.png`。目录当前六题进入 5-12 题资料库层，本题验收覆盖三个专题、解题技巧和开放状态组合筛选、空结果、清除筛选及目录预览真实加载。
- 前端复用审查：`FRONTEND.md` 已把“字符灯廊”登记为 `ChallengeCompletion` 与 `CodePracticeWorkbench` 使用方。字符灯廊场景、技能预演和 `WindowProgramBuilder` 保持题目专用；后者需要“每个扫描字符 / 重复仍存在时”两种嵌套作用域文案及移回技能架，当前共享编排器尚未覆盖完整契约，因此不复制或强行扩展共享 JSX，待下一道真实题出现相同需求时复核。
- 浏览器验收：`work/e2e_character_corridor.mjs` 在桌面 `1440 x 1000`、手机 `390 x 844` 和减少动态效果环境通过，覆盖手动选窗、技能预演、空程序、九技能双层嵌套、鼠标 PointerSensor 拖放、同层/跨层移动、移回技能架、首批播放、三批验证、公开通过但隐藏失败、首次 `9/9` 通关、重复提交/刷新不重播、目录筛选、控制台无错误和无横向溢出。
- 最终门禁：`npm run verify:shared` 通过正式共享登记、共享组件 `2/2`、根产品 `128/128`、零号仓库 `41/41`、全局 TypeScript 和两套生产构建；`dist/games/character-corridor/index.html` 已生成，仅保留既有主包体积提醒。
- 技能记录：本轮使用并记录 `frontend-design` 与 `webapp-testing`，并按 `skill-usage-tracker` 完成调用统计。

### 2026-08-25 - `工作线程2` 部署通知已发送

- 触发事实：“和为 K 的子数组”已完成实现、专项回归、完整共享门禁和桌面/手机浏览器验收，符合“任务真正完成时主动通知部署服务”的新协作约定。
- 通知结果：已向“部署服务”任务 `01a0380a-f6ad-7830-bf3c-5825bc77e36d` 成功发送以“部署通知：”开头的消息，列明新增 `/games/subarray-sum-k/`、预览 `/previews/subarray-sum-k.png`、已做与未做验证、现有包体积提醒和“当前工作区可部署”。
- 部署边界：本线程没有直接修改服务器；公网构建、上传、原子切换与发布后可达性验证由“部署服务”任务继续执行。

### 2026-08-25 - 完成通知首次触发公网发布成功

- 触发来源：已收到工作线程 0、1、2、3、4 与通关反馈同步任务的“部署通知”，确认字符灯廊、频谱滑窗站、前缀和档案站、窗口校准台、共享代码实战与通关反馈等成果可部署。
- 发布执行：未等待或追加完整产品验证，仅执行生成静态发布文件所必需的 `npm run build:all`；构建成功后将当前完整工作区发布为服务器版本 `/var/www/skill-programming/releases/20260825-172141`。
- 切换安全：上传包 SHA-256 校验通过，Nginx 配置检查通过，`current` 已原子切换到新版本；上一版本 `/var/www/skill-programming/releases/20260825-164112` 保留。
- 公网结果：目录 `/`、六道关卡 `/games/zero-warehouse/`、`/games/rainline/`、`/games/minimum-window/`、`/games/subarray-sum-k/`、`/games/find-all-anagrams/`、`/games/character-corridor/` 以及六张对应预览均返回 HTTP 200 和非空内容。
- 后续机制：当前没有定时发布任务；后续仍由其他开发任务完成后主动通知“部署服务”，本任务收到通知后直接构建并更新服务器。
- 持久协作：`AGENTS.md` 已登记同一完成通知规则，后续新开的相关任务也必须向固定“部署服务”任务回报，不依赖本轮逐个下发消息。

### 2026-08-25 - `规则编排下一步提示` 独立预览完成（待用户确认）

- 原型边界：新增 `http://localhost:5174/?hint=preview` 独立预览，只用于确认通用交互方向；未修改真实关卡、正式共享 `SkillProgramBuilder`、`CHALLENGE_PLAYBOOK.md` 功能登记或 `FRONTEND.md` 组件登记。
- 交互契约：玩家主动打开“下一步提示”后，系统只根据当前规则结构给出一个缺失动作及原因，同时高亮推荐技能和合法放置位置；提示本身不添加技能、不移动节点、不注入答案。
- 状态演进：空程序提示“先建立遍历范围”；加入遍历后提示“在遍历里加入判断”；加入判断后提示“补上条件成立后的动作”；三项完整后提示“运行当前批次”。玩家必须分别手动加入 `遍历数据 -> 判断条件 -> 执行动作`，提示才会前进。
- 完成态：运行后提示收敛为“当前批次已验证”，不继续制造错误或强制下一步；关闭提示、清空程序和手机“数据场景 / 规则编排”视图切换均可独立操作。
- 验证结果：根产品生产构建通过；根测试 `25` 个文件、`135/135` 通过；专项真实浏览器验收覆盖桌面 `1440 x 1000`、手机 `390 x 844`、减少动态效果、四段提示、无自动改写、高亮迁移、运行、关闭、清空、控制台无错误和无横向溢出。
- 后续边界：等待用户确认当前视觉与交互版本；确认后再由 `通用功能同步与回归` 任务 `01a037e5-c5c4-71e0-902d-1f5512760daa` 评估正式功能 ID、题目规格接口、共享组件接入和全部真实关卡回归。
- 技能影响：本原型按 `frontend-design` 保持安静的双栏算法工作台、移动端显式视图切换和稳定控件尺寸；按 `ui-copy-polish` 将提示压缩为“下一步动作 + 当前缺口原因”；按 `webapp-testing` 完成真实浏览器专项；调用已由 `skill-usage-tracker` 记录。
- 部署通知：本预览完成后已按协作约定通知“部署服务”任务 `01a0380a-f6ad-7830-bf3c-5825bc77e36d`，如实列明新增查询入口、已做/未做验证、待用户确认边界和“当前工作区可部署”；未提前通知正式功能同步任务。

### 2026-08-25 - `规则编排下一步提示` 完成通知触发公网发布

- 发布执行：收到独立预览完成通知后，未追加全仓测试或真实关卡回归，仅执行静态发布所需的 `npm run build:all`；构建成功并发布为 `/var/www/skill-programming/releases/20260825-173036`。
- 切换安全：上传包 SHA-256 校验和 Nginx 配置检查通过，`current` 已原子切换；上一版本 `/var/www/skill-programming/releases/20260825-172141` 保留用于回滚。
- 公网入口：`http://39.96.62.70/?hint=preview` 返回 HTTP 200；统一目录和六道现有关卡也全部返回 HTTP 200。
- 产品边界：本次只发布待用户确认的独立提示预览，没有把该能力登记或接入为正式关卡通用功能。

### 2026-08-25 - 公网访问故障复核

- 用户反馈公网页面无法正常访问；复核确认 Nginx 仍为 `active`，配置检查通过，`current` 仍指向版本 `/var/www/skill-programming/releases/20260825-173036`，无需回滚。
- 服务器本机与公网访问 `http://39.96.62.70/`、`http://39.96.62.70/games/minimum-window/` 均返回 HTTP 200，访问与错误日志未显示应用故障。
- 原因边界：服务器当前只监听 HTTP 80，未监听 HTTPS 443；`https://39.96.62.70/` 无法完成 TLS 连接。用户访问时必须保留完整 `http://` 前缀，浏览器若自动升级为 HTTPS 会表现为持续加载或无法访问。
- 当前处理：保留现有线上版本和 Nginx 配置，不使用会产生浏览器证书警告的自签名证书；后续若需要 HTTPS，应使用域名与有效证书单独配置。

### 2026-08-25 - 技能编排回归核心逻辑

- 用户确认：后续关卡虽然形式上遵循六阶段规范，但技能被拆成代码级微操作，偏离接雨水与零号仓库已经确认的学习流程；阅读完大量说明也无法形成可记忆的算法骨架。
- 新产品决定：玩家只排布少量核心逻辑块，默认 `4-7` 个；读取临时值、计数加减、索引前进和连续容器操作不再逐个占用技能卡。同一核心技能可以在真实执行时展开多个详细因果帧。
- 分层边界：技能架负责“提炼逻辑并排序”；执行帧负责展示完整状态变化；代码参考、结构槽位、词典和语义检查继续保留实现级细节。概念映射完整不等于技能数量多。
- 规范修正：`AGENTS.md`、`CHALLENGE_PLAYBOOK.md` 和 `FRONTEND.md` 已加入认知压缩门槛，并把新增四关标记为待按精炼规范复核；接雨水和零号仓库作为基准保持现有流程。

### 2026-08-26 - 四道新增关卡完成核心技能收敛

- 当前事实：和为 K 的子数组、字符灯廊、频谱滑窗站、最小覆盖子串已分别从 `7 / 9 / 10 / 19` 个微技能收敛为 `5 / 5 / 5 / 6` 个核心技能；此前完成记录中的旧技能数量是历史实现，已由本节覆盖。
- 玩家排布：四题现在只要求排列算法骨架。读取字符、计算目标旧前缀、频次增减、结果写入和索引前进等实现细节不再单独占技能卡。
- 细节保留：执行器仍在核心技能内部逐帧展示读取、判断、公式、状态更新与边界移动；完整代码参考、词典、结构槽位、语义检查和公开/隐藏用例未被删减。
- 题目简报：四份 `BRIEF.md` 已同步核心技能清单、作用域和 Playbook 功能 ID；`CHALLENGE_PLAYBOOK.md` 覆盖矩阵将四题的精炼技能架复核标记为完成，`FRONTEND.md` 记录 `5 / 5 / 5 / 6` 个核心节点的复用审查事实。
- 自动验证：根产品 `145/145`、零号仓库 `50/50`、共享组件 `4/4`、正式共享登记、TypeScript 和两套生产构建全部通过；仅保留既有 Vite 主包超过 500 kB 的提醒。
- 浏览器验证：四题在单一生产预览服务完成桌面与手机完整流程，字符灯廊另覆盖减少动态效果。验证包括无污染预演、空程序、拖放/点选、核心节点顺序与作用域、完整执行帧、多批验证、结构/自由代码、隐藏失败、首次通关、重复提交、刷新恢复、控制台错误和横向溢出；四条 E2E 均通过。
- 运行环境说明：并行启动多个 Vite 开发服务会竞争同一依赖优化缓存，曾在自由编辑器首次加载时触发开发态 React 模块混用；改用单一生产预览后四题全部通过，生产构建不受该开发态竞争影响。

### 2026-08-26 - 核心技能收敛版本完成公网发布

- 发布前检查：当前完整工作区在 `npm run build:all` 前后源码指纹一致；TypeScript 与两套生产构建成功，仅保留既有 Vite 大包体积提醒。
- 发布版本：构建产物 SHA-256 在服务器端校验通过，发布到 `/var/www/skill-programming/releases/20260826-111939`；Nginx 配置检查通过，`current` 已原子切换到该版本。
- 回滚保障：上一版本 `/var/www/skill-programming/releases/20260825-173036` 保留；本次远端所有本机路由检查均成功，未触发回滚。
- 公网 HTTP：统一目录、六道关卡和六张真实预览共 13 个地址全部返回 HTTP 200 且内容非空。
- 公网浏览器：桌面视口逐页加载 `/` 与六道关卡，页面标题、根应用和正文均正常，无横向溢出、控制台错误或页面异常。
- 当前访问协议：仍使用 `http://39.96.62.70/`；服务器尚未配置 HTTPS。

### 2026-08-26 - `工作线程4`（最小覆盖子串核心技能订正复核）

- 流程复核：重新完整对照最新 `AGENTS.md`、`CHALLENGE_PLAYBOOK.md` 与 `FRONTEND.md`；“窗口校准台”当前技能定义、技能架、作用域契约和标准程序均严格保持 6 个核心节点，页面标题与进度文案同步为“六步 / 6 项”，未恢复此前 19 个微技能设计。
- 细节保留：目标逐字登记、入窗读取与欠账判断、字符余额增减、总欠账更新、窗口长度公式、左右边界前进等实现级动作仍在 6 个核心技能内部生成独立执行帧；29 段参考、原子词典、24 项结构语义和语义检查保持完整。
- 专项验证：本题 `interpreter.test.ts` 与 `codePractice.test.ts` 共 `13/13` 通过；根产品 TypeScript 与七入口生产构建成功，仅保留既有 Vite 主包超过 500 kB 提醒。
- 浏览器复核：使用单一生产预览在桌面 `1440 x 1000` 与手机 `390 x 844` 重跑 `work/e2e_minimum_window.mjs`，通过手动选窗、无污染预演、6 核心节点拖放/点选和嵌套、完整逐帧执行、三批验证、结构/自由代码、首次通关、重复提交、刷新恢复、目录筛选、控制台错误与横向溢出检查。
- 复核结论：未发现需要补改的业务源码、文案或测试遗漏；本轮只追加复核事实，不修改共享组件或覆盖其他任务成果。技能调用已按 `skill-usage-tracker` 记录 `frontend-design` 与 `webapp-testing`。

### 2026-08-26 - `工作线程1`（找到字符串中所有字母异位词核心技能订正复核）

- 流程复核：重新完整对照最新 `AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md` 与本记录；“频谱滑窗站”当前只暴露 `建立目标频谱`、`向右扫描源信号`、`纳入右侧字母`、`超宽时收缩左侧`、`匹配时记录起点` 5 个玩家可编排核心技能，正确程序 fixture 与端到端脚本均明确断言 5 个节点。
- 细节保留：目标字符读取与累计、入窗频次加一、超宽判断、移出频次减一、`left` 推进、26 格频谱比较和结果写入仍在核心技能内逐帧展开，并保留于参考步骤、代码词典、结构槽位、概念链路和 AST 语义检查中；旧 `count-pattern / remove-outgoing / advance-left / record-index` 等名称只作为代码阶段原子映射存在，未泄漏到技能架或玩家规则树。
- 自动验证：本题 `interpreter / suiteValidation / codePractice` 专项 `13/13` 通过；`npm run build:all` 通过全局 TypeScript、根产品与零号仓库两套生产构建，仅保留既有 Vite 大包体积提醒。
- 浏览器复核：在单一生产预览服务上重跑 `work/e2e_find_all_anagrams.mjs`，通过动手选窗、无污染预演、5 节点拖放/点选与嵌套、完整执行帧、三批验证、结构/自由代码、公开与隐藏用例、首次通关、重复提交/刷新防重播、目录预览、接雨水共享编排器回归和桌面/手机无溢出检查；`public/previews/find-all-anagrams.png` 已从本次生产验收刷新并完成视觉核对。
- 复核结论：未发现需补改的业务源码、流程文案或测试遗漏；本轮只追加复核事实并刷新真实目录预览，不修改 `ChallengeCompletion` 或其他共享组件，不覆盖并行任务成果。

### 2026-08-26 - `工作线程0`（无重复字符的最长子串核心技能订正复核）

- 流程复核：重新完整对照最新 `AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md` 与本记录；“字符灯廊”当前只暴露 `初始化灯廊`、`扫描直到末尾`、`重复时收缩窗口`、`纳入当前字符`、`更新最长记录` 5 个玩家可编排核心技能，正确程序 fixture、专项测试和端到端脚本均明确锁定 5 个节点。
- 细节保留：读取当前字符、移出左端字符、频次增减、左右边界推进和 `right - left + 1` 窗口长度公式仍在核心技能内逐帧展开，并完整保留于代码参考、原子词典、结构槽位、概念链路和 AST 语义检查；这些实现级动作没有重新变成技能卡。
- 补漏修复：规则编排完成条此前只统计根节点，双层程序会错误显示“程序节点 2”；`App.tsx` 现改为递归统计整棵规则树，桌面与手机均正确显示“程序节点 5”。专项解释器测试新增五技能精确契约及 `scan` 内部读取字符、推进右边界的执行帧断言，端到端脚本新增旧微技能不可取用和完整手机五节点编排检查。
- 自动验证：字符灯廊专项 `18/18` 通过；最新 `npm run verify:shared` 通过正式共享登记、共享组件 `5/5`、根产品 `154/154`、零号仓库 `50/50`、全局 TypeScript 和两套生产构建，仅保留既有 Vite 主包超过 500 kB 提醒；`git diff --check` 通过。
- 浏览器复核：在最新生产预览上重跑 `work/e2e_character_corridor.mjs`，桌面 `1440 x 1000`、手机 `390 x 844` 和减少动态效果环境全部通过，覆盖五核心技能、内部细节帧、手动选窗、预演、空程序、双层编排、播放与三批验证、隐藏失败、首次通关、防重复播放、控制台错误和横向溢出；最新桌面/手机截图已目视确认节点数为 5，真实目录预览 `public/previews/character-corridor.png` 同步刷新。
- 复用与通关结论：未复制或分叉共享组件，也未修改已完成旧关卡；代码实战继续直接使用唯一共享 `ChallengeCompletion`，保持首次真实公开与隐藏用例全通过后先持久化、再展示，失败、不完整、重复提交和刷新不重播。技能调用已按 `skill-usage-tracker` 记录 `frontend-design` 与 `webapp-testing`。

### 2026-08-26 - `工作线程2`（和为 K 的子数组核心技能订正复核）

- 流程复核：重新完整对照最新 `AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md` 与本记录；“前缀和档案站”当前只暴露 `初始化档案`、`扫描所有数值`、`累加当前前缀和`、`统计匹配区间`、`登记当前前缀和` 5 个玩家可编排核心技能，正确程序 fixture、专项测试和端到端脚本均明确锁定 5 个节点。
- 细节保留：读取当前值、计算 `prefixSum - k`、查询历史前缀次数、答案累加、查询后登记 Map 和探针前进仍在核心技能内逐帧展开，并完整保留于代码参考、原子词典、结构槽位、概念链路和 AST 语义检查；这些实现级动作没有重新变成技能卡。
- 防回归加固：`challenge.test.ts` 新增技能架精确集合断言；`interpreter.test.ts` 新增核心技能内部帧断言；`work/e2e_subarray_sum_k.mjs` 新增五张技能卡、逐项无污染预演、完整内部因果帧和手机流程检查。未修改业务逻辑、共享组件或 `ChallengeCompletion`。
- 自动验证：本题专项 `12/12` 通过；最新 `npm run verify:shared` 通过正式共享登记、共享组件 `5/5`、根产品 `154/154`、零号仓库 `50/50`、全局 TypeScript 和两套生产构建，仅保留既有 Vite 主包超过 500 kB 提醒。
- 浏览器与视觉复核：单一生产预览上的 `work/e2e_subarray_sum_k.mjs` 通过桌面与手机完整流程，覆盖五核心技能、预演不污染程序、逐帧执行、三批验证、结构/自由代码、首次通关、防重复播放、刷新恢复、目录预览、控制台错误和横向溢出；`public/previews/subarray-sum-k.png`、`outputs/subarray-sum-k-program-mobile.png` 与 `outputs/subarray-sum-k-completion-mobile.png` 已目视确认。
- 复用结论：本题继续直接复用正式共享代码工作台和唯一 `ChallengeCompletion`，没有恢复旧 7 个微技能设计，也没有覆盖其他并行任务成果。技能调用已按 `skill-usage-tracker` 记录 `frontend-design` 与 `webapp-testing`。

### 2026-08-26 - `工作线程3`（共享核心技能契约复核完成）

- 复核范围：完整对照最新 `AGENTS.md`、`CHALLENGE_PLAYBOOK.md`、`FRONTEND.md` 与本记录，检查 `AlgorithmChallenge`、正式共享 `SkillProgramBuilder`、正式共享 `CodePracticeWorkbench`、四道精炼题规格、共享登记门禁和测试；四题继续保持 `5 / 5 / 5 / 6` 个核心技能，没有覆盖并行订正。
- 发现与订正：共享 `LogicCodeStep` 的细粒度参考步骤字段仍名为 `skillLabel`，会把最多 29 段代码参考误读成 29 张玩家技能卡；现统一更名为 `stepLabel`，迁移接雨水与四道新增题参考和共享工作台渲染。类型注释明确概念可仅落在内部帧/代码层，一张核心技能卡可关联多个实现概念。
- 防回归：新增跨四题 `coreSkillContract.test.ts`，锁定精炼技能数量、`4-7` 门槛、参考步骤多于技能卡及实现细节无需直接成为技能；共享编排器 fixture 新增“一张核心技能卡承载读取、更新、边界推进三个概念”的断言。`FRONTEND.md` 正式登记和复用审查清单已同步；`CHALLENGE_PLAYBOOK.md` 现有 `skill.core-library / skill.detail-expansion` 契约已完整，无需重复修改。
- 自动验证：目标回归 8 个测试文件 `52/52` 通过；`npm run verify:shared` 通过正式共享登记、共享组件 `5/5`、根产品 `154/154`、零号仓库 `50/50`、全局 TypeScript 与两套生产构建，仅保留既有 Vite 主包超过 500 kB 提醒；`git diff --check` 通过。
- 浏览器验证：单一生产预览 `http://127.0.0.1:5174/` 已启动；和为 K、字符灯廊、异位词、最小覆盖四条完整 E2E 在桌面/手机全部通过，接雨水共享代码参考区另以桌面 `1440 x 1000`、手机 `390 x 844` 验证 12 段参考、无横向溢出和无控制台错误。原 Python 脚本因当前 Python 环境无 Playwright 未执行，已用随附 Node Playwright 覆盖本次受影响界面。
- 发布边界：没有新增或改变公网路由；目录和六道关卡入口保持不变。零号仓库私有旧代码参考类型未纳入共享字段更名，避免修改已完成旧关卡；当前工作区可部署。

### 2026-08-26 - 五项核心技能订正完成公网发布

- 触发来源：已收到工作线程 0、1、2、3、4 的最终部署通知；最终工作区包含四题 `5 / 5 / 5 / 6` 核心技能复核、字符灯廊程序树递归计数修正、共享参考字段 `skillLabel -> stepLabel` 订正，以及三张真实预览刷新。
- 发布前检查：按部署前清理流程检查当前变更、临时调试标记和 `git diff --check`，未删除或改写并行成果；最终 `npm run build:all` 通过，构建前后发布源码指纹均为 `60a1b725a37af286de1c820e582a0b7babf9fd0c1f0d0b51d6a57733581e494a`，仅保留既有 Vite 大包体积提醒。
- 发布版本：最终构建包 SHA-256 为 `809828d3271b4ee2761e189e50ca316414a81694fb5ee6b23bc826f99165ae59`，服务器端校验通过并发布到 `/var/www/skill-programming/releases/20260826-113634`；Nginx 配置检查通过，`current` 已原子切换且服务保持 `active`。
- 回滚保障：切换前版本 `/var/www/skill-programming/releases/20260826-113210` 保留；服务器本机 13 个入口健康检查全部成功，未触发自动回滚。`113210` 是等待剩余订正通知期间的中间版本，现已由最终版本覆盖。
- 公网验证：`http://39.96.62.70/`、六道 `/games/*` 关卡和六张 `/previews/*` 真实预览全部返回 HTTP 200 与非空内容；字符灯廊、异位词、最小覆盖三张本轮刷新预览的公网 SHA-256 与本地文件完全一致。
- 当前协议：公网仍只支持 HTTP；HTTPS 尚未配置。部署临时上传包已从服务器删除，本地临时发布包在记录完成后清理。
- 技能影响：本次使用 `pre-push-cleanup` 约束发布前检查范围，仅审查并保留正式测试、验收脚本和现有并行改动；调用已由 `skill-usage-tracker` 记录。

### 2026-08-26 - `工作线程0`（字符灯廊双角色具象化）

- 用户反馈：字符灯廊此前只用两个 `L / R` 圆点表达左右指针，虽然状态可读，但缺少雨线峡谷巡线员和零号仓库双机器人那样与场景职责一致的角色特征。用户确认本题应补齐同等级的具象表达。
- 角色设计：右边界改为上轨“巡灯员”，使用红色巡查机身、探照灯和信号探针表达逐格读取与向右巡查；左边界改为下轨“守窗员”，使用黄色守卫机身和门闸护臂表达驻守窗口左缘、清退重复字符与收紧边界。两者同站一格时仍保持独立上下岗位轨道，不再退回抽象标记。
- 跨阶段一致性：新增题目专用 `CorridorOperator`，主执行场景和五项技能预演引用同一角色源码；初始化、巡查、读取、清退、移动和抵达末端状态均有对应姿态。运行消息、技能说明、代码参考世界动作、代码词典、算法概念图和 `BRIEF.md` 同步建立“守窗员 -> left / 巡灯员 -> right”的映射，算法结构与 5 个核心技能未改变。
- 复用边界：角色、专用 SVG 与动作全部留在字符灯廊题目模块，没有修改正式共享组件、复制其他关卡角色或覆盖雨线峡谷与零号仓库；`FRONTEND.md` 既有“字符灯廊场景与技能预演保持题目专用”决定继续成立，无需改变正式登记。
- 自动验证：字符灯廊专项 `18/18` 通过；最终 `npm run verify:shared` 通过正式共享登记、共享组件 `5/5`、根产品 `154/154`、零号仓库 `50/50`、全局 TypeScript 和两套生产构建，仅保留既有 Vite 主包超过 500 kB 提醒。
- 浏览器与视觉验收：最终生产预览 E2E 通过桌面 `1440 x 1000`、手机 `390 x 844` 与减少动态效果环境，新增断言锁定两名角色、拒绝旧 `L / R` 标记，并分别验证巡灯员移动探照与守窗员门闸清退状态；主场景、两段动作预演和手机长页截图已逐张目视确认无重叠、遮挡或横向溢出，`public/previews/character-corridor.png` 已刷新为真实角色版本。
- 技能记录：本轮使用 `frontend-design` 明确两个角色的场景身份、独立轮廓和动作因果；使用 `webapp-testing` 完成真实生产预览与截图验收；调用已由 `skill-usage-tracker` 记录。

### 2026-08-26 - 字符灯廊双角色版本完成公网发布

- 发布内容：收到工作线程0部署通知后，将“守窗员 -> `left` / 巡灯员 -> `right`”双角色场景、五项技能预演、同步文案与刷新后的真实预览作为当前完整工作区发布；算法和 5 个核心技能契约保持不变。
- 发布前检查：按部署前清理流程检查当前变更、临时调试标记与 `git diff --check`，未发现需删除的临时代码或冗余实现；正式测试、验收脚本与并行成果全部保留。`npm run build:all` 通过，构建前后发布源码指纹均为 `54507a683267a40871d1c552cabc8b8a95ddebc306c561b4cfa894e69471aee6`，仅保留既有 Vite 大包体积提醒。
- 发布版本：构建包 SHA-256 为 `50e1fe2cd9beeacbbb70f51c5e8f79792752d26415a7a996cb55555527c388b9`，服务器端校验通过并发布到 `/var/www/skill-programming/releases/20260826-121755`；Nginx 配置检查通过，`current` 已原子切换且服务保持 `active`。
- 回滚保障：上一版本 `/var/www/skill-programming/releases/20260826-113634` 保留；服务器内部目录、六道关卡和六张预览共 13 个入口全部通过，未触发自动回滚。
- 公网验证：上述 13 个公网地址全部返回 HTTP 200 与非空内容；线上字符灯廊脚本已确认包含“守窗员”“巡灯员”新角色文案，`/previews/character-corridor.png` 的公网 SHA-256 与本地文件同为 `701b1e7100c9a28b38a1862125192cbede3c4dbb8e2766ccbd211b562273cc9b`。
- 当前协议：公网入口继续使用 `http://39.96.62.70/`，尚未配置 HTTPS。服务器上传临时包已删除，本地临时发布包在记录完成后清理；`pre-push-cleanup` 调用已由 `skill-usage-tracker` 记录。

### 2026-08-26 - 公网实例操作系统无响应诊断

- 用户现象：用户反馈公网程序无法访问；公网根目录及字符灯廊、接雨水、零号仓库均在 TCP 连接成功后返回空响应，非单一路由或静态资源故障。
- 网络证据：实例公网 IP `39.96.62.70` Ping 连续 4 次成功且无丢包，22/80 端口连续 5 轮均可建立 TCP；HTTP 发送请求后由远端主动断开，SSH 在服务端版本握手前以 `kex_exchange_identification: Connection closed by remote host` 断开。HTTPS 仍未配置，不属于本次新增故障。
- 云平台证据：阿里云 ECS 控制台显示实例 `i-2ze9nrbfsbb1rccd6p9n` 为“运行中”，健康状态“正常”，系统可达性与实例可达性检查均通过，CPU 约 `2.07%`，实例未关机且无 CPU 打满迹象。
- 当前判断：故障位于实例操作系统或系统服务层，SSH 与 Nginx 均无法完成应用层响应；不是发布目录丢失、安全组端口未开放或整台云实例停止。由于 SSH 已无法进入，不能在系统内只重启 Nginx。
- 处理边界：恢复需通过云控制台重启整台实例，或启动会收集操作系统诊断数据的一键诊断；本轮用户只询问服务是否停止，尚未授权整机重启或向云平台提交诊断数据，因此未执行有状态操作。
- 技能记录：本轮使用 `chrome:control-chrome` 读取已登录的 ECS 实例状态与监控，调用已由 `skill-usage-tracker` 记录。

### 2026-08-26 - 阿里云安全组与实例绑定复核

- 绑定确认：安全组 `sg-2zee4plvszhphtj8hfhu` 的“实例列表”明确包含实例 `i-2ze9nrbfsbb1rccd6p9n`，公网 IP 为 `39.96.62.70`，实例状态为“运行中”；不是检查了一个未绑定的安全组。
- 入站策略：HTTP `80` 为“允许”、来源 `0.0.0.0/0`、优先级 `1`；SSH `22` 为“允许”、来源 `0.0.0.0/0`；ICMP 同样允许，页面未见拒绝规则。
- 结论：阿里云安全组没有关闭 22/80 端口。结合两端口均可建立 TCP、但 HTTP 返回空响应且 SSH 在协议握手前由远端关闭，故障继续指向实例操作系统或系统服务层。
- 操作边界：本轮仅只读核对控制台，没有修改安全组、没有执行一键诊断、没有重启实例。

### 2026-08-28 - `release 分支本地成果归档上传`

- 归档范围：将当前统一产品目录、六道关卡路由与真实预览、四道新增关卡、共享通关/技能编排/代码实战能力、多语言代码实战、部署配置、正式测试与浏览器验收脚本纳入 `release` 分支版本历史。
- 上传前清理：密钥模式与临时调试标记扫描无异常；正式验收脚本中的通过标记日志和语义实现中的临时变量保留。根目录 `skill-usage-over-3-2026-08-25.zip` 是独立技能统计报告，保留在本地但不进入项目提交；`server-credentials.env` 继续由 `.gitignore` 排除。
- 完整门禁：`npm run verify:shared` 通过，包含共享登记检查、共享组件 `5/5`、根产品 `154/154`、零号仓库 `50/50`、全局 TypeScript 与两套生产构建；仅保留既有 Vite 主包超过 500 kB 提醒。
- 分支状态：上传前已抓取远程状态，本地 `release` 与 `origin/release` 基线一致，无需合并远程提交；本轮不修改 `main`。
- 技能记录：本轮已使用并记录 `pre-push-cleanup`、`connect` 与 `skill-usage-tracker`。
