# 技能编程 Agent Rules

## Project Scope

- 本项目把算法思考过程转化为可操作、可观察、可调试的世界规则，并在最后将场景动作映射为真实代码。
- 产品共同工作区与当前活跃产品源码均位于 `/Users/edy/Documents/ChatGPT/技能编程`；“零号仓库”保留为 `apps/zero-warehouse` 下的独立关卡应用，旧目录 `/Users/edy/Documents/Codex/2026-08-20/wo-xi` 只作为迁移前历史源码，不再承载统一产品入口。
- 本项目是一个拥有统一挑战目录、关卡路由和返回路径的完整软件，不是若干独立关卡页面的集合；新题完成后必须进入同一个产品目录和玩家流程。
- 开始相关工作前先读取 `会话协作记录.md`；发生重要决定、实现完成、验证结果变化或阻塞后及时写回。
- 完成会改变统一产品或公网内容的任务后，当前任务必须主动向“部署服务”任务 `01a0380a-f6ad-7830-bf3c-5825bc77e36d` 发送以“部署通知：”开头的完成消息，列明任务名称、完成内容、新增或变化路由、已做/未做验证、已知问题和“当前工作区可部署”；使用任务间消息触发发布，不创建定时监控。缺少完整验证不阻止通知，但必须如实披露；任务中途暂停不得发送可部署通知。
- “部署服务”每次执行公网发布时，必须把服务器发布与代码仓库同步视为同一个发布批次：先按 `pre-push-cleanup` 检查当前完整可部署工作区，确认私密凭据和临时产物未被纳入，再将同一源码快照提交并推送到已配置的 `origin/release`，并用该快照完成服务器发布与公网验证。最终发布记录必须同时列出服务器版本、Git 提交和推送结果；任一环节失败都不得把该批次报告为完整发布成功。
- 开始新关卡、补齐现有关卡、修改阶段流程或通用玩家功能前，必须完整读取 `CHALLENGE_PLAYBOOK.md`，并按功能 ID 和交付清单维护实现、验收与覆盖矩阵。
- 创建或修改关卡前端、共享组件、页面布局、响应式行为、技能编排、运行反馈或代码实战界面前，必须完整读取 `FRONTEND.md`。
- 只修改题目算法、解析器、语义验证或测试且不影响任何界面契约时，可以不读取 `FRONTEND.md`；一旦变更会影响前端输入、状态或反馈，仍必须读取。
- 新的组件复用边界通过至少两道真实题目验证后，更新 `FRONTEND.md` 和 `会话协作记录.md`；不要只在某个实现文件或单次对话中留下决定。
- 新的通用功能点、默认状态或玩家流程经用户确认后，必须更新 `CHALLENGE_PLAYBOOK.md` 的功能登记表、状态契约或流程清单；不得只在单题中实现。
- 每个新关卡进入 Definition of Done 前，当前负责该关卡的 Agent 必须完成一次前端复用审查，并更新 `FRONTEND.md` 中的正式组件登记表或复用评估队列；未完成审查不得交付。
- 修改正式共享组件时，当前执行修改的 Agent 同时是本次组件库更新负责人，必须识别全部使用关卡、迁移契约、运行受影响关卡回归并记录结果；不得在单题内复制一个分叉版本规避回归。
- 复用决定若会改变玩家的学习步骤、场景隐喻、主要交互或已经确认的视觉体验，必须先由用户确认；纯内部实现且保持体验契约不变时，由负责 Agent 按 `FRONTEND.md` 执行。
- 当前第一关是 Java“移动零”，但新增算法时必须沿用本文件的方法论，不能复制一套独立做题页面。
- V1、V2 技能阶段和代码实战阶段是同一产品链路。修改共享能力时必须回归既有版本，不得只验证新页面。

## Global Standards

- 完整遵循 `/Users/edy/.codex/global-agent-rules.md`，其内容视为本文件的全局工程、前端交互和测试标准。
- 本文件只补充技能编程产品特有的方法论与约束；若有明确冲突，以本文件的项目规则为准。

## Core Methodology

### 1. Separate the Engine from the Challenge

- 将系统设计为“通用做题引擎 + 每题声明式规格”，不要设计成“自动理解任意算法”。
- 通用引擎只负责稳定流程：参考区、代码词典、结构填写、自由编写、草稿、运行、提交、错误定位、进度和响应式布局。
- 每道算法只负责自己的事实：场景、技能、策略、方法签名、代码结构、槽位、标准参考、语义规则、语言能力和测试用例。
- 新算法需要新增题目规格和题目语义验证器；不应复制或分叉通用页面。
- 只有至少两道题出现相同需求后，才把该能力提升到共享抽象；不要为了假设中的未来题目预先构建复杂推断系统。

### 2. Use an Explicit Challenge Contract

每道题应逐步收敛为等价于以下契约的数据和模块：

```ts
interface AlgorithmChallenge {
  id: string
  title: string
  strategy: StrategyDefinition
  languageId: string
  concepts: AlgorithmConceptDefinition[]
  scene: SceneDefinition
  manualStage: ManualStageSpec
  skills: SkillDefinition[]
  automationStage: {
    programContract: SkillProgramContract
    verificationBatches: VerificationBatch[]
  }
  codePractice: {
    methodSignature: string
    scaffold: StructuredScaffold
    slots: StructuredSlotDefinition[]
    referenceSteps: LogicCodeStep[]
    mappings: CodeMappingEntry[]
    publicCases: CodePracticeCase[]
    hiddenCases: CodePracticeCase[]
  }
  validate(program: ProgramAst): SemanticResult
}
```

- 共享页面只消费契约，不直接判断题目 ID、变量名或某道题的代码字符串。
- 若新增一题必须在共享 JSX 中增加多处分支，先检查缺失的规格字段或扩展点，不要继续堆条件判断。
- 题目规格是产品事实源；结构渲染、代码组合、左侧参考和测试 fixture 应尽可能消费同一份数据，避免多份答案漂移。

### 3. Build a Complete Algorithm Concept Map

- 拆题时不得只列“动作流程”或“代码行”。先建立算法概念清单，再让场景、技能、运行帧、代码参考、结构槽位和语义验证共同引用这些概念。
- 标准参考中每一个有算法含义的变量都是独立映射点，包括输入、输出、指针、边界、累加器、持久状态、容器和临时变量。不得只把它藏在一句动作描述或长代码中。
- 对每个变量至少声明：稳定角色 ID、世界中对应的对象或特征、标准代码名、初始值、读取点、写入/更新时机、更新规则和对结果的影响。
- 持久跨步骤、参与分支决策、决定公式、控制遍历或累加结果的关键状态，必须在前置场景中持续可见，并在变化帧中明确显示“旧值 -> 触发条件 -> 新值”。仅局部辅助的临时变量可不常驻场景，但仍必须进入代码词典或参考映射。
- 除变量外，每题必须映射：输入/输出与原地副作用、核心运算与公式、控制流与作用域、状态更新顺序、分支决策依据、循环不变量/当前保证、终止与边界条件，以及数据结构/API 原子操作。
- 公式不能只显示结果。必须展示操作数来自哪个已映射状态、当前值是什么、执行什么运算、产生什么新值，以及新值写回哪里。
- 更新时机是映射的一部分。“移动 -> 更新状态 -> 使用新状态计算 -> 累加”这类因果链必须能在执行帧中单步观察，并可分别映射到代码；但不要求每个内部状态变化都成为玩家可编排技能。玩家编排只保留能表达算法骨架的核心逻辑块，块内细节由运行帧展开。
- 同一概念在各阶段使用同一稳定 ID。场景实体/读数、技能、追踪帧字段、左侧参考步骤、代码词典、右侧槽位和语义检查应显式关联；不在 JSX 或文案中靠顺序和名称猜测对应关系。
- 标准参考中任一影响结果的概念，若在前置阶段不可观察、不可执行或没有对应映射，则该关卡映射不完整；前置技能中任一影响结果的动作，若没有对应代码步骤或语义检查，同样不完整。

题目规格中的概念节点应等价于：

```ts
interface AlgorithmConceptDefinition {
  id: string
  kind: 'input' | 'output' | 'state' | 'operation' | 'control' | 'api' | 'boundary'
  label: string
  worldMeaning: string
  canonicalCode: string
  lifecycle?: {
    initialization: string
    reads: string[]
    writes: string[]
    updateRule: string
  }
  links: {
    sceneIds: string[]
    skillIds: string[]
    traceFields: string[]
    referenceStepIds: string[]
    mappingEntryIds: string[]
    structuredSlotIds: string[]
    semanticChecks: string[]
  }
}
```

- 以上是责任契约，不强制所有题使用完全相同的 TypeScript 字段。题目可按需拆分模块，但不能丢失上述信息和可追溯关联。
- 右侧玩家可自定义变量名；语义验证器可在本次验证内将它们识别为上述稳定角色 ID，但不反向改写左侧标准参考。

### 4. Keep the Learning Layers Connected

- 每道题先定义四层概念：场景对象、技能动作、算法策略、关卡目标。
- 每个可执行技能必须映射到一段职责明确的代码结构或连续语句；一个技能可以承载玩家自然理解为同一步的内部细节。数据结构/API 方法仍作为独立代码映射项，例如 `Map.put` 可以位于某个核心技能内部，但不能从代码词典和参考中消失。
- 左侧参考严格按玩家已经验证过的技能执行顺序呈现，清楚表达循环、判断和动作的嵌套关系。
- 右侧结构填写只锁定已经在技能阶段确认的程序骨架，开放真正需要玩家理解和填写的语义槽位。
- 通关依据必须是程序的真实语义和测试结果，不得匹配唯一答案文本。

### 5. Keep References Canonical and Stable

- 左侧完整逻辑代码、代码词典和复制内容使用题目定义的标准示例变量，保持稳定、可教学、可测试。
- 右侧允许玩家自定义合法变量名和等价写法；语义验证器应根据 AST、作用域和数据流判定正确性。
- 不实现“根据玩家代码自动改写左侧变量名”的功能，也不维护最后一次变量识别状态。
- 题目语义验证可以为了判定正确性识别快指针、慢指针等角色，但识别结果不得反向修改教学参考。
- 不根据变量声明顺序猜测角色，不使用固定变量名或正则字符串匹配代替语义分析。

### 6. Separate Structure, Semantics, and Execution

- 结构层回答“程序树是否完整”：作用域、循环、判断、缩进、固定符号和槽位位置。
- 语义层回答“逻辑是否正确”：变量角色、边界、条件、交换数据流、状态更新和作用域关系。
- 执行层回答“结果是否正确”：在公开和隐藏用例上运行，并比较真实输出。
- 三层可以给出不同错误，但必须指向玩家可修正的位置；结构槽位诊断应映射并聚焦到对应输入。
- 初次进入和重置后保持中性状态。只有玩家运行、提交或完成明确交互后，才显示需要修正的错误。

### 7. Make Language Support Explicit

- 解析器和执行器按语言隔离；题目规格声明自己需要的语法能力。
- 当前 Java 子集只支持已实现的变量、数组、数组访问、`length`、`for`、`if/else`、赋值、比较、算术、自增和交换等能力。
- 新算法需要 `while`、递归、集合或对象 API 时，应先扩展解析、诊断和执行能力，再开放对应题目。
- 不伪装成完整 JVM，也不对未实现语法静默降级；必须给出明确的“不支持”反馈。
- 不使用 `eval` 或不受控代码执行；执行器必须有步数或时间限制。

## Product Catalog Contract

- 软件根路径是统一挑战目录；`src/product/challenges.ts` 是目录展示事实源。完成并开放的新关卡不得另建脱离目录的入口，也不得只完成题内页面而遗漏目录登记、真实预览、独立路由、构建入口和返回目录路径。
- 目录元数据与题目算法规格职责分离。目录只描述玩家选择关卡所需的信息，不承载场景状态、技能程序、完成条件或算法验证逻辑。
- 从下一道新题开始，每个目录条目必须声明：稳定 ID、排序、世界标题、算法标题、学习专题、数据结构、解题技巧、难度、开放状态、目标语言、阶段数、预计时长、前置关卡、摘要，以及显式的预览与路径状态。`available` 必须提供真实游戏预览和可达关卡路径；其他状态可以显式留空，但不得使用虚构截图或无效链接。现有题目在目录 V2 实现时迁移到同一契约。

目录条目应逐步收敛为等价于以下契约：

```ts
interface ChallengeCatalogEntryV2 {
  id: string
  order: number
  worldTitle: string
  algorithmTitle: string
  summary: string
  trackId: string
  dataStructureIds: string[]
  techniqueIds: string[]
  difficulty: 'intro' | 'intermediate' | 'challenge'
  status: 'available' | 'coming-soon' | 'locked'
  languageIds: string[]
  stageCount: number
  estimatedMinutes: number
  prerequisiteIds: string[]
  preview: { src: string; alt: string } | null
  route: string | null
}
```

- 分类使用两个维度：学习专题/路径是目录的主要组织方式；数据结构、解题技巧、难度和开放状态是筛选维度。不得把“双指针”等策略和“数组”等数据结构混成一个不可扩展的单一分类字段。
- 开放状态至少区分 `available`、`coming-soon` 和 `locked`。只有真实可玩的关卡才能标记为开放；锁定必须对应明确前置条件，开发中或锁定条目不得伪装成可点击关卡。
- “上次进入”只表示最近访问。完成度、当前阶段、通关状态和推荐下一题只有接入真实持久化事实后才能展示，不得由访问次数、目录顺序或占位数据推断。
- 目录按真实题量逐步提升信息密度：1-4 题保持真实预览主导的精选卡片；5-12 题增加专题分组和基础筛选；超过 12 题增加搜索、排序和继续学习入口。阈值可根据真实浏览器验收调整，但跨过一个层级前必须完成相应设计与验收，不能无限延长当前两题布局。
- 提前建立元数据与状态契约，不提前渲染没有真实使用价值的搜索、筛选、分页或虚构进度。只有真实题量达到对应层级时才实现并开放控件。
- 新增关卡或修改目录契约时，必须验证目录空态、单题、多题、长标题、缺失/失败预览、各开放状态、桌面和窄屏布局；同时检查目录登记、页面路由和生产构建入口一致，不允许出现“目录可见但无法打开”或“关卡可运行但目录不可发现”。
- 目录分类、筛选、搜索、排序、状态或进度属于通用产品功能。经用户确认后必须同步更新 `CHALLENGE_PLAYBOOK.md` 的功能 ID、`FRONTEND.md` 的目录体验契约、自动测试和 `会话协作记录.md`，不得只在 `ProductHome.tsx` 中临时实现。

## Default Challenge Blueprint

以下内容是从第一题中验证出来的默认题型蓝图。创建下一题时先采用这些默认值；只有算法、学习目标或设备体验确实要求变化时才偏离，并在题目规格与协作记录中写明原因。

- 默认的是阶段职责、交互形式和验证标准，不是第一题的具体数量。技能数、验证批次数、结构槽位数、嵌套深度和测试用例数全部由题目规格决定。

### 1. Default Stage Flow

1. **动手理解**：首屏直接进入可操作场景，不增加营销页或大段教程。玩家先用最直接的动作完成一组小数据，观察目标状态和约束。
2. **技能认识**：把算法提炼成少量核心技能。每个技能表达一个需要玩家记住和排序的逻辑决定，并可单独预演；读取临时值、索引自增、单次容器读写等实现细节不单独占用技能卡。
3. **规则编排**：玩家将技能组合成可重复执行的程序树。循环、判断等控制技能拥有明确作用域，动作技能只能进入满足契约的位置。
4. **调试执行**：真实执行玩家规则，逐步展示状态变化。失败反馈应指出技能、参数、作用域或数据结果中的具体问题。
5. **多批验证**：使用多组公开数据验证规则的泛化能力。通过全部正式批次后才解锁代码实战；开发环境可以保留直接进入代码页的验收入口。
6. **代码实战**：玩家把已经验证的技能树翻译为代码。默认进入结构填写，同时保留自由编写；最后通过公开与隐藏用例判定通关。

- 各阶段应共享同一算法事实，不能让场景动画、技能执行、代码参考和测试判定分别维护互相冲突的逻辑。
- 每次阶段转换都保留明确返回路径，玩家可以从代码实战返回技能调试，而不是被锁在单向流程中。
- 下一阶段只在当前阶段目标真实完成后开放；不要用点击次数、动画播放结束或固定答案字符串冒充完成条件。

### 2. Default Game Presentation

- 采用安静、清晰、工作台式的算法场景。视觉服务于状态观察和重复操作，不使用营销式 Hero、装饰性卡片堆叠或与题目无关的背景效果。
- 桌面端默认使用左右工作区：左侧是数据场景与执行结果，右侧是技能架、参数和规则树。两侧处于同一主层级，避免在卡片中继续嵌套卡片。
- 移动端不要把桌面双栏机械压缩。使用明确的视图切换或纵向流程，确保场景、规则树和主要操作各自获得可用宽度与高度。
- 数组、节点、指针、容器和状态标记使用稳定尺寸与明确位置，动态标签、动画和错误状态不得改变主布局尺寸。
- 抽象角色应尽可能具象化。存在两个同时工作的指针时，默认使用两个独立可见角色，不用“一个角色 + 一个抽象标记”表达双主体协作。
- 动画必须展示因果链，例如“读取 -> 判断 -> 取出 -> 移动 -> 放入 -> 指针前进”，不能只播放与真实状态无关的装饰效果。
- 同位操作、空输入、无动作和边界状态要有符合算法语义的专门表现，不能制造一次实际上没有发生的交换或移动。

### 3. Default Skill Model

- 技能保持认知上的单一职责：一个技能承担一个玩家能够命名、记住和排序的核心算法动作或控制结构。不要把整套答案封装为一个万能技能，也不要把读取字符、计数加减、索引前进等代码级微操作逐个做成技能。
- 默认把玩家需要排布的技能控制在 `4-7` 个；超过 `7` 个时必须先合并同一意图下的连续微操作，并在题目简报中说明仍需超出的学习理由。数量限制只约束玩家技能，不限制执行帧、概念节点、参考代码或语义检查的完整度。
- 一个核心技能可以在运行时展开多个真实因果帧。例如“收缩左侧窗口”内部可以依次展示读取左字符、更新计数和移动左边界；玩家只排布一次“收缩左侧窗口”。
- 每个技能定义稳定类型和可重复实例；实例包含唯一 ID、参数、父作用域、子技能和运行状态。
- 控制技能默认显式拥有作用域与契约。循环负责重复和遍历上下文，判断负责条件分支，动作技能声明自己需要的数据与上下文能力。
- 编排器至少支持拖动放置、点选放置、同层重排、跨层嵌套、移回技能架和移动端触控；插入位置必须可见且命中区域不能互相遮挡。
- 技能预演只用于观察含义，不加入规则树、不修改正式数组、不改变批次结果。真实执行与预演可以有不同节奏，但必须表达同一语义。
- 运行、暂停和重置期间锁定会破坏当前执行一致性的编辑操作；执行结束或重置后恢复编辑。
- 规则验证先检查参数与作用域契约，再执行数据；错误应尽量关联到具体技能实例，而不是只给一个全局失败提示。

### 4. Default Code Practice Presentation

- 桌面端默认使用左右工作台：左侧代码参考，右侧目标语言编辑与运行；当前默认语言是 Java。移动端默认提供“参考 / 编辑”显式切换，不依赖窄屏横向滚动。
- 左侧默认有两个标签：主标签“逻辑代码”和次标签“代码词典”。主标签负责完整组合路径，词典负责场景对象、技能动作、语法结构和数据结构/API 的原子查询。
- “逻辑代码”在玩家零输入时就完整可见，顺序严格对应技能树。每段至少包含技能名称、世界动作、逻辑作用、作用域层级和可组合代码。
- 参考轨道必须覆盖形成完整方法体所需的所有内容，包括初始化、控制结构、动作语句和闭合边界；按顺序组合后必须可以运行。
- 每段代码可以独立复制，并显示短暂复制成功反馈；复制只写入剪贴板，不自动插入右侧，不替玩家完成题目。
- 左侧使用题目定义的标准变量和标准写法，始终保持稳定。右侧允许变量改名和等价实现，但不会反向修改左侧参考或词典。
- 右侧默认提供“结构填写 / 自由编写”两种模式，并默认选中结构填写。两种模式草稿独立保存，切换不覆盖，重置只清空当前模式。
- 结构填写由题目 scaffold 渲染固定方法签名、控制结构、缩进、符号和作用域；只开放语义槽位。固定结构与输入槽位在视觉、焦点和辅助技术层面必须清楚区分。
- 结构槽位不显示答案型 placeholder。完整答案只出现在左侧参考；右侧空槽通过标签说明职责，输入时必须正常保留空格。
- 自由编写默认显示固定方法签名与不可编辑外壳，编辑器只承载方法体；提供语法高亮、行号、键盘编辑、错误行定位和明确的语言子集说明。

### 5. Default Run, Submit, and Feedback Flow

- 初始空白和重置后显示中性状态，不标红、不提前展示“规则未满足”。
- “运行当前用例”只运行玩家当前选择的公开用例，并展示输入、预期、实际结果和公开标记。
- “提交全部用例”运行全部公开与隐藏用例。成功时显示通过数量；失败时只展示按用例顺序遇到的第一个反例，明确给出输入、期望、实际结果和失败原因，不展开后续失败用例。隐藏用例不得提前展示，但提交失败后允许揭示当前第一个失败的隐藏反例。
- 结构模式提交前先做槽位必填、语法和题目语义检查；遇到问题聚焦第一个可修正槽位。自由模式将解析或运行诊断定位到对应代码行。
- 进度依据 AST、作用域和数据流中的语义检查，不依据标准变量名或代码字符串；进度只提供反馈，不隐藏左侧参考。
- 语法正确不等于算法正确。必须继续执行真实用例，避免“通过编译”被误判为“通过题目”。
- 重置当前模式不影响另一个模式的草稿；刷新后恢复上次模式和对应草稿；旧版本预填答案不得通过历史存储键重新出现。
- 保持运行、提交、重置和返回技能调试为清晰可见的主命令。按钮状态、禁用条件和反馈文本必须与真实执行状态一致。

### 6. Default Content and Copy Structure

- 关卡标题直接表达题目世界或算法对象，不使用空泛口号。
- 场景说明优先描述玩家当前要观察的状态和目标，不在界面上重复讲解所有功能或键盘操作。
- 技能卡默认只显示短名称和一句逻辑作用；世界动作、触发条件和内部状态变化放入主动打开的预演。玩家不阅读全部预演，也应能看懂并记住核心编排顺序。
- 错误文案说明“哪里不对、为什么不对、下一步改哪里”，避免只显示“失败”“语法错误”或内部解析器信息。
- 完成反馈区分当前用例通过、全部公开通过、全部公开与隐藏通过，不把不同层级的成功混为一谈。
- 数据结构/API 词典按原子能力建模。数组 `length`、未来的 `Map.put` 等应是独立条目，而不是只作为某个长代码片段的一部分。

## New Challenge Brief Template

开始开发下一题前，先补齐以下题目简报。缺失项可以标记待确认，但不得在共享页面中用硬编码临时猜测。

```markdown
# 题目名称

## Algorithm Contract
- 输入类型与方法签名：
- 目标输出或原地修改结果：
- 允许修改的数据：
- 时间/空间目标：
- 必须处理的边界：

## Learning Goal
- 数据结构：
- 核心技能（默认 4-7 个）：
- 组合策略：
- 玩家最终应能解释的因果关系：

## Algorithm Concept Map
- 输入、输出与原地副作用：
- 核心运算、控制流、不变量、边界与 API：

| Concept ID | Kind | 场景特征 | 标准代码 | 初始化 | 读取/更新规则 | Skill / Trace | Reference / Mapping / Slot / Check |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

## World Model
- 场景隐喻：
- 可见实体及其代码角色：
- 初始状态：
- 目标状态：
- 合法动作与禁止动作：

## Skill Program
- 技能清单及参数：
- 控制作用域：
- 技能前置条件：
- 技能执行效果：
- 预演表现：

## Code Practice
- 目标语言与所需语法：
- 标准变量和标准参考：
- 锁定 scaffold：
- 可编辑槽位：
- 允许的等价实现：
- 独立 API 映射：

## Verification
- 手动阶段完成条件：
- 自动阶段验证批次：
- 公开代码用例：
- 隐藏与边界用例：
- 必须拒绝的典型错误实现：
```

## Recommended Challenge Module Layout

添加第二道算法题时，优先把题目特有代码收敛到独立目录，并让共享页面通过规格读取：

```text
src/challenges/<challenge-id>/
  challenge.ts       # AlgorithmChallenge 总规格与导出
  scene.ts           # 场景实体、初始状态和目标
  skills.ts          # 核心技能、参数、作用域与内部执行展开
  semantics.ts       # AST、作用域和数据流语义验证
  cases.ts           # 公开、隐藏和边界用例
  reference.ts       # 标准参考步骤与原子映射
  qaFixtures.ts      # 正确、等价和典型错误实现
```

- 共享的场景组件、技能编排器、代码工作台、解析器和执行器留在通用目录。
- 迁移第一题时保持行为与验收不变；不要为了目录整洁顺带重写已经通过验证的交互。
- 题目注册表只负责通过 ID 获取 `AlgorithmChallenge`，不承载题目业务逻辑。

## New Challenge Workflow

新增算法题时按以下顺序工作：

1. 定义学习目标：明确玩家最终要理解的数据结构、技能和策略。
2. 设计世界规则：每个指针、容器和状态都有可观察的场景表达，每个技能有明确前置条件和效果。
3. 建立技能树：先让玩家通过操作和调试验证算法逻辑，再进入代码阶段。
4. 声明题目规格：方法签名、结构 scaffold、可编辑槽位、参考步骤和原子映射全部进入数据模型。
5. 实现题目语义验证：基于 AST、作用域和数据流识别正确逻辑，接受变量改名和等价写法。
6. 准备真实用例：包含公开示例、空输入、最小输入、极端分布、边界值和能区分错误算法的隐藏用例。
7. 建立共源参考：标准参考代码由题目规格组合，并确保通过全部用例；不要在产品和 QA 中维护互相独立的答案。
8. 接入通用页面：共享 UI 只消费题目规格；结构填写和自由编写使用独立草稿，重置只影响当前模式。
9. 完成真实浏览器验收：逐键输入、空格、复制、运行、提交、错误聚焦、刷新恢复、重置、桌面和移动端均需检查。
10. 回归既有内容：运行单元测试、类型检查、生产构建、V1/V2 主流程和现有题目专项验收。

## Complexity Guardrails

- 优先解决学习目标和做题闭环，不为提示装饰引入跨槽位推断、隐式状态或难以解释的自动行为。
- 一个能力若需要每道题持续编写大量特例，应留在题目模块，而不是伪装成通用引擎能力。
- 通用引擎不得依赖“移动零”、`nums`、`scanIndex`、`writeIndex` 等当前题目细节。
- 题目语义模块可以包含题目特有概念，但必须通过清晰接口返回检查结果和诊断，不得直接操作页面状态。
- 使用结构化数据和 AST；不要通过 JSX 硬编码完整 scaffold，也不要用临时字符串替换实现代码个性化。
- 删除已取消功能时，应同时删除状态、生成器、测试和文档中的当前契约；历史决定可保留在协作日志，但必须写明已被新决定覆盖。

## Verification Requirements

- 标准参考代码必须通过全部公开和隐藏用例。
- 至少准备一份使用不同变量名或等价交换方向的正确实现，确认语义验证不依赖固定答案。
- 至少准备一份能通过部分公开用例但会在隐藏边界失败的错误实现，确认测试具有区分度。
- 结构模式应验证：固定框架不可破坏、槽位键盘可达、空格可输入、错误能聚焦、初始与重置状态中性。
- 自由模式应验证：空白方法体、语法诊断、不支持语法、正确提交、隐藏失败、草稿恢复和当前模式重置。
- 左侧应验证：零输入即可查看完整标准参考；每段复制不自动写入编辑器；代码词典的 API 条目保持独立。
- 响应式页面至少验证常用桌面和窄屏移动视口，无重叠、横向溢出或被固定控件遮挡的内容。

## Definition of Done

一套新算法逻辑只有同时满足以下条件才算接入完成：

- 场景、技能、策略、代码结构和真实运行结果形成可解释的闭环。
- 题目通过声明式规格接入，没有复制通用页面或把题目细节散落到共享 JSX。
- 标准参考完整可组合，自定义合法实现也能通过语义与用例验证。
- 标准参考中的所有结果相关概念都能通过稳定 ID 追溯到场景、技能/运行帧、词典、结构槽位和语义检查。
- 关键状态的初始化、每次更新和作为公式操作数的过程已完成单步和视觉验收。
- 错误实现会在语义检查或边界用例中失败，并能得到可操作反馈。
- 桌面、移动端、刷新、草稿、复制、运行、提交和重置流程均完成浏览器验收。
- 既有题目与 V1/V2 回归通过，协作记录已写回最新事实。
- 已完成 `FRONTEND.md` 要求的前端复用审查；若涉及正式共享组件，所有登记使用方均已迁移并通过规定回归。
