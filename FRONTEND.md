# 技能编程前端复用规则

## Purpose

- 本文件定义算法关卡之间的前端复用边界，目标是复用真正稳定的交互能力，而不是追求最高复用率。
- 本文件中的组件名表示推荐职责边界，不代表当前源码已经存在同名导出，也不要求立即重构第一题。
- 默认选择是先保留在题目模块；只有满足复用门槛后，才提升为共享组件。

## Core Principle

- 复用“完成关卡的稳定交互骨架”，保留“表达算法含义的具体场景”。
- 两个页面视觉相似、都使用数组或都属于双指针，不足以证明组件应该共享。
- 共享组件负责稳定流程和交互状态；题目规格负责数据、文案、技能、算法语义、测试用例和场景事实。
- 不为减少少量 JSX 引入大量配置、条件分支或难以解释的 render props。
- 无法明确判断时先留在题目模块，等下一道真实题目再次出现相同需求后复核。

## Learning Compression Gate

- 共享交互骨架不能迫使题目把代码级微操作全部暴露为技能。玩家首先需要看懂并排布算法骨架，内部执行器再把每个核心技能展开为完整因果帧。
- 玩家技能默认保持 `4-7` 个。连续服务于同一意图的“读取 -> 更新计数 -> 移动索引”应合并为一个核心技能；只有它们代表不同算法选择、顺序错误会改变策略，或需要玩家独立控制时才拆开。
- 技能卡只承载短名称和一句逻辑作用。触发条件、世界动作、前置状态、公式和多步结果放入主动预演或运行轨迹，不在技能架重复展示。
- 概念清单、代码词典和语义检查可以比技能架细得多。复用审查必须明确区分“玩家可编排节点”和“内部可追溯步骤”，不得用映射完整性证明技能越多越好。
- 新关卡进入前端验收前，先做一次无预演扫读：只看技能架时，玩家能否用一句话复述正确顺序；如果必须逐张读完长说明才能编排，视为未通过。

## Reuse Gate

一个前端能力只有同时满足以下条件，才允许提升为共享组件：

1. 至少两道已经实现的真实题目出现相同需求，不根据假设中的未来题目预先抽象。
2. 两题中的用户目标、交互生命周期、空状态、运行状态、错误状态和完成状态基本一致。
3. 题目差异可以通过声明式数据或少量职责明确的插槽注入，不需要在共享 JSX 中判断题目 ID。
4. 抽取后能减少重复的行为、状态或测试，而不只是减少相似标签和 CSS。
5. 共享组件可以使用两道题的 fixture 独立验证，不依赖某道题的全局变量或页面状态。
6. 抽取不会削弱题目场景的辨识度、动画因果链或算法表达。

任一条件不满足时，选择以下更窄的方案：

- 只共享无界面的状态控制器或数据结构，题目继续渲染自己的界面。
- 只共享按钮、进度、反馈条等小型视觉原语。
- 完全保留为题目专用组件，并记录下一次复核触发条件。

## Update Triggers

以下事件会强制触发组件库复用审查或更新，不依赖用户额外提醒：

| Trigger | Required action | Completion gate |
| --- | --- | --- |
| 新关卡前端达到可验收状态 | 与现有题目逐项执行 Reuse Gate，更新正式登记表或评估队列 | 审查未记录时，关卡不得进入 Definition of Done |
| 第二道真实题目出现相同交互 | 比较状态机、移动端行为、错误和完成状态，决定保持专用、进入评估或准入 | 不得只以视觉相似为理由抽取 |
| 正式共享组件的接口或行为变化 | 修改唯一共享源码，识别并迁移全部登记使用方 | 全部使用方回归通过后才算完成 |
| 正式共享组件出现确认缺陷 | 优先修复共享源码并检查所有使用方是否受影响 | 不得只在报告缺陷的题目中打补丁 |
| 组件被替代或不再适用 | 标记废弃、迁移全部使用方并删除无引用实现 | 不保留无所有者的共享分叉 |

## Ownership and Approval

- 当前正在开发或修改关卡的 Agent，是该任务的复用审查负责人。
- 当前直接修改正式共享组件的 Agent，是本次组件库更新负责人。即使协调其他任务执行回归，也仍对使用方清单、迁移完整性和最终记录负责。
- 负责人必须完成判断、实现、旧使用方迁移、测试、浏览器验收、登记表更新和协作记录写回，不能只提出抽取建议后结束任务。
- 纯内部复用且不改变既有体验契约时，由负责人按本文件直接执行。
- 若复用会改变学习步骤、场景隐喻、主要交互、题目表达或用户已经确认的视觉版本，必须先取得用户确认，不能用“组件统一”替代产品决定。
- 如果统一接口会迫使题目提供大量例外或改变算法表达，应停止晋升并把能力留在题目模块；这不视为复用失败。

## Component Lifecycle

组件只允许处于以下一种状态：

1. `题目专用`：只服务一道题，或明确承载题目场景与算法语义，不进入组件库。
2. `复用评估`：至少两题出现相似需求，但接口、状态或体验尚未完成验证，源码继续留在题目模块。
3. `准入待迁移`：已经通过 Reuse Gate，但尚未形成唯一共享源码，仍不属于正式组件库。
4. `正式共享`：存在唯一共享源码、稳定契约、明确使用方和双题以上测试，所有使用方直接引用它。
5. `废弃`：已有替代方案，停止新增使用方；完成迁移后删除源码和登记项。

状态只能按以下条件推进：

- `题目专用 -> 复用评估`：第二道真实题目出现相同需求。
- `复用评估 -> 准入待迁移`：完成两题对照并满足全部 Reuse Gate。
- `准入待迁移 -> 正式共享`：抽取唯一源码、迁移所有使用方，并完成规定回归。
- 任意非废弃状态可以退回 `题目专用`，前提是记录不适合复用的具体原因。
- 只有 `正式共享` 状态属于通用组件库；其他状态都不得被描述为“组件库已经提供”。

## Official Shared Component Registry

本表只登记已经存在唯一共享源码的正式组件。登记使用方必须直接引用公开导出，不得复制分叉版本。

| Component | Shared source | Contract | Consumers | Last verified |
| --- | --- | --- | --- | --- |
| `ChallengeCompletion` | `src/shared/completion/ChallengeCompletion.tsx`；由 `src/shared/completion/index.ts` 公开导出 `ChallengeCompletion` 与 `ChallengeCompletionProps` | 只消费 `passedCount`、`total` 和 `onClose`，呈现已确认的六阶段“算法回路闭合”视觉；不拥有题目通关判定，不导入题目 ID、场景实体、变量或算法专用动画 | 雨线峡谷 / 接雨水；零号仓库 / 移动零 V2；前缀和档案站 / 和为 K 的子数组；窗口校准台 / 最小覆盖子串；字符灯廊 / 无重复字符的最长子串；频谱滑窗站 / 找到字符串中所有字母异位词；产品目录显式预览也直接引用同一源码 | 2026-08-25；频谱滑窗站再次验证首次 `9/9` 真实提交、先保存后展示、重播、关闭、`Esc`、重复提交与刷新不重播、桌面/手机稳定终态 |
| `CodePracticeWorkbench` | `src/components/CodePractice.tsx` 默认导出；契约位于 `src/challenges/types.ts` 的 `AlgorithmChallenge.codePractice` | 消费题目 runtime、presentation、scaffold、reference、mapping、case、checkToSlot 和 challenge ID 隔离草稿；细粒度参考使用 `stepLabel`，不把每个代码步骤建模成玩家技能；负责结构/自由模式、参考、运行提交、首个反例、完成事实与共享通关反馈，不读取题目 ID 分支 | 雨线峡谷 / 接雨水；前缀和档案站 / 和为 K 的子数组；窗口校准台 / 最小覆盖子串；字符灯廊 / 无重复字符的最长子串；频谱滑窗站 / 找到字符串中所有字母异位词 | 2026-08-26；四道新增题完整桌面/手机 E2E 与接雨水双视口参考区回归通过；根产品 `154/154` |
| `SkillProgramBuilder` | `src/shared/program/SkillProgramBuilder.tsx`；由 `src/shared/program/index.ts` 公开导出组件、节点和标签类型 | 消费声明式核心技能、程序树、活动节点、编辑锁定、题目文案、节点工厂和变更回调；单张技能卡可关联多个实现概念；负责拖放、点选放置、嵌套作用域、同层/跨层移动、删除、清空和执行高亮，不拥有题目程序合法性或验证状态 | 雨线峡谷 / 接雨水（经 `src/components/ProgramBuilder.tsx` 薄适配）；频谱滑窗站 / 找到字符串中所有字母异位词 | 2026-08-26；共享 fixture `3/3` 验证空态、作用域与一张核心技能卡承载多个概念；完整共享门禁通过 |

正式组件晋升后必须立刻替换“暂无”行，并记录：

- 唯一源码位置和公开导出名称。
- 契约或类型定义位置。
- 所有直接使用关卡，不允许只写“多个关卡”。
- 最近一次完整回归日期与对应协作记录。
- 若使用方清单或契约变化，修改组件的同一任务必须更新本表。

## Reuse Evaluation Queue

本表不是通用组件库清单，只记录尚未形成正式共享源码的复用判断。候选名称表示职责边界，不代表当前已经存在同名组件导出。

| Component role | Status | Evidence / next decision | Shared responsibility |
| --- | --- | --- | --- |
| `ChallengeCatalogNavigation` | 复用评估 | 根目录已形成唯一 Catalog V2 清单；六题达到资料库层后，三个专题分组、五类筛选、结果数量、清除与空态已经完成桌面/手机验收，各题页头与阶段层级仍保持独立 | 挑战登记、真实预览、选择跳转、状态、题量适配、上次进入记录和返回目录 |
| `ChallengeShell` | 复用评估 | 接雨水、和为 K 子数组、最小覆盖子串与无重复字符的最长子串都有六阶段与双工作区，但世界品牌、阶段状态推导和移动布局仍服务各自算法表达 | 只评估页面尺寸、响应式工作区和阶段容器 |
| `AdvancedProgramBuilderCapabilities` | 复用评估 | 正式共享 `SkillProgramBuilder` 已覆盖接雨水与频谱滑窗站的通用树交互；和为 K、字符灯廊、异位词、最小覆盖已分别收敛为 `5 / 5 / 5 / 6` 个核心节点，不再以大量微操作和深层作用域证明需要高级编排器 | 少量核心节点的作用域、移回技能架和题目能力契约 |
| `PlaybackControls` | 复用评估 | 四道新增题均在精炼技能后完成播放、暂停、单步和重置回归；内部详细帧保持完整，但帧节奏、按钮编排和完成文案仍不同 | 播放控制、帧进度和执行锁定 |
| `BatchVerifier` | 准入待迁移 | 接雨水、和为 K 子数组与最小覆盖子串确认空程序禁用、逐批播放、直接校验、首个失败数据、编辑失效、成功解锁和重新验证；等待统一源码边界 | 批次进度、当前程序全批校验、失败入口和全部通过反馈 |
| `StructuredCodeEditor` | 复用评估 | 当前由正式共享 `CodePracticeWorkbench` 内部复用；只有出现独立于工作台的真实使用方时再评估单独导出 | 数据驱动结构渲染和槽位交互 |
| `LogicReferencePanel` | 复用评估 | 当前由正式共享 `CodePracticeWorkbench` 内部复用；完整逻辑、词典、复制和语义进度已跨接雨水与和为 K 子数组验证 | 标准参考轨道与原子词典交互 |
| `SkillPreviewDialogShell` | 复用评估 | 弹窗生命周期可共享，但技能动画和讲解必须保持题目专用 | 焦点、关闭、重播和响应式弹窗外壳 |

题目专用组件不进入本队列。当前明确保持专用的包括“移动零”的仓库场景、“接雨水”的峡谷地形场景、“和为 K 的子数组”的数值带/档案柜场景、“最小覆盖子串”的字符带/欠账台和技能预演、“无重复字符的最长子串”的字符灯廊场景与技能预演，以及“频谱滑窗站”的源信号带、双 26 格频谱和技能预演。此前以 19 个微操作构成的最小覆盖编排器已收敛为 6 个核心节点，不再作为复用方向。

## Product Shell Boundary

- `src/product/challenges.ts` 是当前挑战目录的唯一登记清单；`src/product/ProductHome.tsx` 只负责选择体验，不承载题目算法、完成条件或阶段状态。
- 六道已登记题目通过独立多页面入口加载各自的场景与样式，避免为了统一目录让全局 CSS 或题目 JSX 互相渗透。
- 已开放关卡使用当前可验收版本的真实游戏截图；开发中或锁定条目可以不提供预览，但不得使用与实际玩法无关的占位图，也不得渲染无效链接。
- “上次进入”只代表最近访问，不等于学习进度。真实进度进入产品契约前，目录不得显示虚构百分比、完成阶段或通关标记。
- `ChallengeCatalogNavigation` 尚未形成跨题唯一 JSX，因此仍处于复用评估，不进入正式共享组件登记表。

## Catalog V2 Information Architecture

### Metadata And Taxonomy

- 目录卡片消费统一目录条目，不从题目 JSX、路由名称或摘要文字推断分类和状态。
- 学习专题/路径是一级组织，例如“双指针入门”“搜索与遍历”；数据结构、解题技巧、难度和开放状态是独立筛选维度。
- 同一道题可以属于一个主要学习专题并拥有多个数据结构或技巧标签。标签使用稳定 ID 和集中定义的显示名称，不在卡片中自由填写近义分类。
- 难度使用 `intro / intermediate / challenge` 三档，对应“入门 / 进阶 / 挑战”，表达玩家完成该学习关卡所需的认知复杂度，不直接照搬外部题库评级。
- 状态至少包含 `available`、`coming-soon` 和 `locked`。开放卡片可以进入；开发中卡片不能显示“开始挑战”；锁定卡片必须显示真实前置条件。
- 预计时长是辅助选择信息，不是完成倒计时。前置关卡使用稳定 challenge ID，不以显示顺序建立隐式依赖。

### Density By Challenge Count

- 1-4 题：保留当前真实预览主导的精选卡片，允许两列桌面和单列移动布局；重点帮助玩家理解每个算法世界的差异。
- 5-12 题：目录进入资料库形态。增加专题分组和基础筛选，卡片收紧摘要与元信息高度，但继续保留能识别实际玩法的真实预览。
- 13 题及以上：增加标题/算法搜索、排序和真实继续学习入口；搜索与筛选组合后必须有明确结果数量、清空条件和无结果状态。
- 题量阈值是默认触发线，不应直接散落为多个 JSX 魔法数字；若经真实内容验收调整，应集中声明并同步产品清单。
- 分页、无限滚动或虚拟列表只在真实性能或浏览成本证明需要时引入。学习目录优先保持专题结构和当前位置可理解。

### Layout And Interaction

- 桌面端让专题标题、筛选控件、结果数量和卡片网格共享稳定对齐线；卡片预览、标题、摘要、标签和命令在同一密度层级保持稳定高度。
- 移动端不横向压缩桌面筛选栏。主要专题使用可读的单一选择入口，其余维度进入明确的筛选面板；应用筛选后仍显示当前条件和清除入口。
- 分类和筛选使用菜单、分段选择、复选框等符合选项性质的控件；不把所有标签都做成同层按钮，不使用重复状态文案挤占卡片。
- “最近进入/继续学习”可以置于目录前部，但最近进入只读访问记录；继续学习必须读取真实阶段或通关记录，两者在数据和文案上严格区分。
- 长标题、长摘要、多标签、缺失或加载失败的预览、空目录、单题、筛选无结果、全部锁定和混合状态都是正式响应式验收状态。
- 卡片整体只有在状态允许进入时才作为链接；不可玩状态不得依靠拦截点击伪装成链接。键盘焦点、状态文本和前置条件必须可感知。

### Current Implementation Boundary

- 当前产品已实现六道题所需的 Catalog V2 元数据，并进入 5-12 题资料库层：按三个学习专题分组，提供数据结构、解题技巧、难度、开放状态和专题筛选，以及结果数量、清除条件和无结果状态；尚未实现 13 题以上才需要的搜索、排序或真实学习进度。
- 新题继续通过统一条目接入，不新增固定题目数量、固定专题或固定开放状态分支；达到第 13 道可展示题前完成搜索层设计与验收。
- 第 5 道和第 13 道可展示题分别触发资料库层与搜索层实现；未达到真实题量前只保留契约和测试准备，不展示无效控件。

## Keep Challenge-Specific

以下内容默认不得做成跨题万能组件：

- 完整场景组件，例如仓库货位、峡谷地形、链表节点网络或图搜索地图。
- 手动理解阶段的具体任务、选择方式和完成条件。
- 机器人、巡线员、货箱、雨水等世界角色及其专用动画。
- 技能参数表单、技能预演正文和算法专用提示。
- 算法运行帧的业务字段、成功条件、语义验证和错误规则。
- 题目专用文案、颜色含义和具有算法语义的布局关系。

数组格子、节点、指针标记等视觉原语也不要仅因名称通用就立刻抽取。只有第三个实际场景再次需要相同尺寸、状态和交互时，才考虑形成小型原语库。

## Component Contract Rules

- 共享组件不得导入某道题的 `model`、`cases`、变量名或场景常量。
- 共享组件不得出现 `challengeId === ...`、题目名称判断或算法代码字符串分支。
- 共享组件通过稳定类型接收规格和状态，不自行猜测技能角色、变量角色或算法语义。
- 题目专用渲染应位于题目模块；共享组件只提供职责清晰的插槽，避免形成一个拥有大量布尔参数的万能组件。
- 若一个共享组件需要多个题目分别提供大量 JSX 回调，优先检查是否只应共享状态控制器，而不是共享整个界面。
- 共享组件的默认文案只能表达通用操作；世界叙事、学习提示和算法反馈由题目规格提供。
- 共享组件不拥有题目完成条件。它只呈现题目验证器返回的结构化状态和可执行动作。

## Recommended Frontend Layout

```text
src/
  product/             # 挑战目录清单与产品选择入口
  shared/
    challenge/       # ChallengeShell、阶段与响应式工作区
    program/         # SkillProgramBuilder 与通用树交互
    runtime/         # PlaybackControls、BatchVerifier、通用反馈
    code-practice/   # CodePracticeWorkbench、结构编辑与参考区
    ui/              # 已被多处验证的小型基础控件
  challenges/
    <challenge-id>/
      components/    # 场景、手动阶段、技能预演和题目专用 UI
      challenge.ts   # 声明式题目规格
      scene.ts
      skills.ts
      semantics.ts
      cases.ts
      reference.ts
```

- 目录只是推荐结果，不为整理目录而迁移已经通过验收的代码。
- 共享目录中的组件必须能够在不导入任何题目模块的情况下编译和测试。
- 题目模块可以依赖共享组件，共享组件不得反向依赖题目模块。

## Extraction Workflow

新增关卡时按以下顺序判断组件复用：

1. 先读取题目简报和现有共享组件契约，直接复用已经通过两题验证且职责吻合的组件。
2. 对没有现成契约的题目 UI，先在题目模块中完成真实流程，不提前扩展共享组件。
3. 与已有题目逐项比较用户目标、状态机、交互、响应式行为和错误反馈，而不是只比较 JSX 外观。
4. 只有通过 Reuse Gate 后才抽取；抽取时先定义最小数据契约，再迁移新题和旧题共同使用。
5. 迁移后分别回归所有使用该组件的题目，确认场景表现和学习路径没有被通用化削弱。
6. 将新的共享边界、例外和验证结果写回本文件及 `会话协作记录.md`。

## Automated Enforcement

文档负责触发和责任，统一源码与自动测试负责实际同步。建立第一个正式共享组件时，负责该次晋升的 Agent 必须在同一任务中完成以下基础设施：

- 建立唯一共享源码目录或共享包，所有登记使用方直接引用该源码，不复制文件。
- 为正式组件提供至少两道题的 fixture 或集成测试，覆盖登记表中的全部使用方。
- 提供统一的共享回归命令；修改共享目录时，该命令必须运行所有登记使用方的测试和构建。
- 在项目具备 CI 时配置共享目录变更触发上述回归；没有 CI 时，由负责人本地执行同一命令并把结果写入协作记录。
- 自动检查应验证正式登记项具有真实源码、公开导出、使用方和测试；登记表与源码不一致时检查失败。

在上述基础设施真正落地前，不得声称组件库已经“自动更新”或“自动回归”。当前自动化状态为：`ChallengeCompletion` 与 `SkillProgramBuilder` 已纳入本地统一门禁 `npm run verify:shared`，会检查正式登记、唯一源码、公开导出、各自两个真实使用方与共享 fixture，并运行全仓测试和两套生产构建；仓库当前未配置 CI，因此共享目录变更仍由组件库更新负责人本地执行并记录结果。

正式共享组件更新的标准流水线为：

```text
共享源码发生变化
  -> 根据正式登记表确定全部使用方
  -> 运行共享组件测试
  -> 运行全部使用方测试与构建
  -> 对受影响交互执行桌面和移动端浏览器验收
  -> 更新登记表最近验证时间与会话协作记录
  -> 全部通过后完成更新
```

## Review Checklist

创建或审查共享组件时确认：

- 新增第三道题是否只需要新增题目规格和题目组件，而无需修改共享 JSX 的题目分支？
- 组件是否真的共享了行为或状态，而不是只共享样式相近的容器？
- 空白、编辑、运行、暂停、失败、成功和重置状态在所有使用方是否含义一致？
- 桌面和移动端是否都保持题目自己的主要观察目标？
- 题目专用变量、算法字段、文案和动画是否仍留在题目模块？
- 共享组件是否拥有针对至少两道题 fixture 的测试？
- 共享契约是否把玩家核心技能与内部执行帧、代码参考步骤分开命名和建模，避免把每个参考步骤重新解释成技能卡？
- 删除任一题目模块后，共享组件是否仍然不包含该题残留知识？
- 正式登记表、真实源码、公开导出、使用方清单和回归范围是否一致？
- 当前任务是否已经满足触发条件下的责任和完成门槛？

## Explicit Non-Goals

- 不构建可以自动适配任意算法的万能页面。
- 不追求所有关卡使用完全相同的场景布局或动画模板。
- 不因一次重复就创建抽象层。
- 不用题目 ID 分支、巨型配置对象或大量布尔参数伪装复用。
- 不为了组件数量、目录整齐或代码行数指标牺牲可读性和题目体验。
